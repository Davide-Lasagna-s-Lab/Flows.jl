var documenterSearchIndex = {"docs":
[{"category":"section","location":"linearised/#Linearised-dynamics","page":"Linearised dynamics","text":"This page is the most important conceptual chapter in the manual: it explains, in depth, the multiple ways Flows.jl lets you integrate the tangent and adjoint of a nonlinear dynamical system, when to prefer each, and why.\n\nLinearisation problems are a recurring need in dynamical-systems theory and PDE-constrained optimisation. Concrete uses include\n\nSensitivity analysis with respect to initial conditions or parameters;\nGradient computation of trajectory-integrated objective functionals;\nStability analysis (Floquet, Lyapunov exponents);\nNewton–Krylov continuation of periodic orbits and equilibria;\nOptimal control via adjoint-based gradient descent;\nVariational data assimilation (4D-Var).\n\nWhat sets Flows.jl apart is that it exposes four distinct integration paths for these linearisations, each with a different trade-off between consistency, memory, and accuracy. This page maps the four paths to the underlying mathematics, derives the difference between them, and offers concrete guidance on which to pick for which problem.","title":"Linearised dynamics"},{"category":"section","location":"linearised/#The-two-equations","page":"Linearised dynamics","text":"We assume the reader has read Mathematical foundations; we briefly restate the equations for reference.\n\nLet the primal nonlinear system be\n\ndotmathbfx(t) = mathcalLmathbfx(t) + mathbff(tmathbfx(t)) qquad mathbfx(t_0) = mathbfx_0","title":"The two equations"},{"category":"section","location":"linearised/#Tangent-equation","page":"Linearised dynamics","text":"A first-order perturbation mathbfy(t) of the primal obeys the linear, non-autonomous equation\n\ndotmathbfy(t) = mathcalLmathbfy(t) + mathbff_mathbfx(tmathbfx(t))mathbfy(t) qquad mathbfy(t_0) = mathbfy_0\n\nIt runs forward in time and depends on the primal trajectory through the Jacobian mathbff_mathbfx.","title":"Tangent equation"},{"category":"section","location":"linearised/#Adjoint-equation","page":"Linearised dynamics","text":"The adjoint state mathbfp(t) obeys\n\n-dotmathbfp(t) = mathcalL^*mathbfp(t) + biglmathbff_mathbfx(tmathbfx(t))bigr^*mathbfp(t) qquad mathbfp(T) = mathbfp_T\n\nIt runs backward in time, uses the transpose Jacobian, and again depends on the primal trajectory.\n\nBoth equations are linear in the linearisation state and non-autonomous in time through their dependence on the primal trajectory. The numerical difficulty is therefore entirely in how the primal trajectory is communicated to the linearised integrator. This is where Flows.jl offers a choice.","title":"Adjoint equation"},{"category":"section","location":"linearised/#The-four-integration-paths","page":"Linearised dynamics","text":"Every linearised integration path in Flows.jl is one of the four cells in the following 2×2:\n\n Tangent (forward) Adjoint (backward)\nDiscretely consistent (replay stages) ContinuousMode(false) → DiscreteMode(false) DiscreteMode(true)\nContinuous ContinuousMode(false) ContinuousMode(true)\n\nThere is also a fifth path: coupled forward integration of the primal and a tangent perturbation together as a Coupled{2} state. We discuss it in the section Coupled (forward-mode tangent) below because it has a different cost profile from the four above.\n\nThe five paths fall on a continuum of what is communicated:\n\n   primal trajectory stored as          coupled forward             ← cheapest in passes\n                                       integration of (x, y)\n                                       (no stored trajectory)\n       ─────────────────────────────────────────────────────────────\n                                       continuous, RAMStorage\n       ─────────────────────────────────────────────────────────────\n                                       discrete, RAMStageCache\n                                                                    ← most memory\n\nThe rest of the page discusses each in turn.","title":"The four integration paths"},{"category":"section","location":"linearised/#Coupled-(forward-mode-tangent)","page":"Linearised dynamics","text":"The simplest and most allocation-frugal path. Pair the primal mathbff with the tangent right-hand-side mathbff_mathbfxmathbfy and integrate the augmented state (mathbfxmathbfy) as one Coupled:\n\nfracmathrmdmathrmdtbeginpmatrixmathbfxmathbfyendpmatrix =\nbeginpmatrixmathcalLmathbfx + mathbff(tmathbfx)mathcalLmathbfy + mathbff_mathbfx(tmathbfx)mathbfyendpmatrix\n\nIn Flows.jl:\n\nf_primal(t, x, dxdt)            = …\nf_tangent(t, x, dxdt, y, dydt)  = …    # default call dep: sees (x, y)\n\nF = flow(couple(f_primal, f_tangent),\n         RK4(couple(zeros(3), zeros(3))),\n         TimeStepConstant(0.01))\n\nx = rand(3)\ny = ones(3)\nF(couple(x, y), (0.0, 1.0))\n\nProperty Value\nDirection Forward\nConsistency with primal Exact (same scheme, same step, same time)\nMemory cost One extra full-state buffer per stage of the scheme\nForward / backward passes One forward pass\nAdjoint capability None. This path only computes tangent perturbations.\nUse when You want the action of J^tau(mathbfx_0) on one (or a few) initial perturbations\n\nThe coupled path is the right answer when:\n\nyou have a forward sensitivity to compute,\nthe number of perturbations you care about is small (one or two, say),\nyou do not need an adjoint (i.e. you do not need the gradient of a scalar with respect to all components of mathbfx_0).\n\nIt is not the right answer for gradient computation over high-dimensional initial conditions: each independent perturbation costs another tangent integration, while a single adjoint pass computes the full gradient.","title":"Coupled (forward-mode tangent)"},{"category":"section","location":"linearised/#Coupled-with-quadrature","page":"Linearised dynamics","text":"The coupled idiom extends naturally to trajectory integrals: couple in a quadrature component that integrates whatever observable you need. The cookbook's Lyapunov exponent of the Lorenz attractor example pairs a primal, a tangent, and a logarithmic quadrature, and reads the Lyapunov estimate out of the quadrature component.","title":"Coupled with quadrature"},{"category":"section","location":"linearised/#Discrete-tangent-and-adjoint","page":"Linearised dynamics","text":"The discretely-consistent path is the rigorous one. The idea is to run the primal once, record every internal stage value of every step into a RAMStageCache, and then drive the linearised flow by replaying the exact same sequence of (t, Δt, stages) triples that the primal saw.","title":"Discrete tangent and adjoint"},{"category":"section","location":"linearised/#Tangent:-replay-forwards","page":"Linearised dynamics","text":"# 1. primal: cache the stages\ncache = RAMStageCache(nstages(RK4), zeros(3))\nF     = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nx     = rand(3); F(x, (0.0, 1.0), cache)\n\n# 2. tangent: replay forwards\ny = randn(3)\nT = flow(f_tan,\n         RK4(zeros(3), DiscreteMode(false)),\n         TimeStepFromCache())\nT(y, cache)              # marches y through cache.ts in order\n\nThe tangent right-hand-side receives the cached stage values explicitly:\n\nf_tan(t, x_stage, y, dydt)   #  x_stage is one of the cached stages\n\ni.e. the integrator hands the linearised step exactly the primal stage at which the Jacobian should be evaluated. This is what makes the scheme discretely consistent.","title":"Tangent: replay forwards"},{"category":"section","location":"linearised/#Adjoint:-replay-backwards","page":"Linearised dynamics","text":"q = zeros(3)              # terminal condition\nA = flow(f_adj,\n         RK4(zeros(3), DiscreteMode(true)),\n         TimeStepFromCache())\nA(q, cache)               # marches q through cache.ts in reverse\n\nThe adjoint right-hand-side has the same signature as the tangent one, but the schemes invert their internal stage order so that the discrete update is the exact transpose of the primal update at each step.","title":"Adjoint: replay backwards"},{"category":"section","location":"linearised/#Why-this-matters","page":"Linearised dynamics","text":"The discrete-adjoint identity holds: for any inner product langlecdotcdotrangle on mathcalX,\n\nbigllanglemathbfp_0mathbfy_Tbigrrangle = bigllanglemathbfq_0mathbfy_0bigrrangle\n\nwhere mathbfy_T is the discrete-tangent state at T starting from mathbfy_0, and mathbfq_0 is the discrete-adjoint state at 0 starting from terminal mathbfp_0 at T. The identity is exact at the level of floating-point: no O(Delta t^p) slack.\n\nThis matters for two classes of algorithms:\n\nNewton–Krylov solvers that use the action of the discrete Jacobian and its transpose interchangeably. A discrete inconsistency between the two breaks the symmetry of the bilinear form and degrades the Krylov convergence.\nLine-search optimisers with strict Armijo conditions or trust-region updates. They demand that the gradient be the exact derivative of the discretised objective, not of the continuous objective evaluated on the discrete state.","title":"Why this matters"},{"category":"section","location":"linearised/#Cost","page":"Linearised dynamics","text":"Property Value\nDirection Forward (tangent) or backward (adjoint)\nConsistency with primal Exact, including floating-point round-off\nMemory cost NS × num_steps full-state buffers in the cache\nForward / backward passes One primal + one linearised pass\nUse when You need the adjoint identity to hold exactly\n\nThe memory cost is the dominant concern. For an integration of N steps with a scheme of mathrmNS stages on a state of size mathcalX, the cache stores roughly NcdotmathrmNScdotmathcalX floats. For DNS-scale problems this is impractical, which leads to the next path.","title":"Cost"},{"category":"section","location":"linearised/#Continuous-tangent-and-adjoint","page":"Linearised dynamics","text":"The continuous path approximates the primal trajectory by an interpolant of step-end snapshots, and integrates the linearised equation against that interpolant. It is the right choice when the state is too large to admit the stage cache.","title":"Continuous tangent and adjoint"},{"category":"section","location":"linearised/#Setup","page":"Linearised dynamics","text":"# 1. primal: record step-end snapshots\nstore = RAMStorage(zeros(3); degree=5)\nF     = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nx     = rand(3); F(x, (0.0, 1.0), store)\n\nThe storage uses a Lagrange polynomial of the configured degree to interpolate between samples. Degree 5 is a good default; higher degree improves accuracy at the cost of stencil width near the boundaries (see Trajectory data).","title":"Setup"},{"category":"section","location":"linearised/#Tangent:-forward-against-the-interpolant","page":"Linearised dynamics","text":"y = randn(3)\nT = flow(f_tan_cont,\n         RK4(zeros(3), ContinuousMode(false)),\n         TimeStepFromStorage(0.005))                  # may differ from primal Δt\nT(y, store, (0.0, 1.0))\n\nThe continuous tangent right-hand-side receives the interpolated primal state as an extra argument:\n\nf_tan_cont(t, x_interp, y, dydt)\n#                ^^^^^^^^ interpolated from the storage by the scheme\n\nThe scheme calls store(buf, t_stage) internally to fill x_interp at each stage time before evaluating the user's right-hand-side.","title":"Tangent: forward against the interpolant"},{"category":"section","location":"linearised/#Adjoint:-backwards-against-the-interpolant","page":"Linearised dynamics","text":"q = zeros(3)\nA = flow(f_adj_cont,\n         RK4(zeros(3), ContinuousMode(true)),\n         TimeStepFromStorage(0.005))\nA(q, store, (1.0, 0.0))                              # decreasing span\n\nThe integrator detects the decreasing span and flips the sign of Δt internally. The interpolation is the same as in the tangent case; only the marching direction and the user-supplied right-hand-side change.","title":"Adjoint: backwards against the interpolant"},{"category":"section","location":"linearised/#What-\"continuous\"-sacrifices","page":"Linearised dynamics","text":"There is no discrete-adjoint identity for this path. Two errors enter:\n\nInterpolation error. The Lagrange interpolant of degree d has an O(Delta t^d+1) error at non-storage times. The linearised scheme sees, in effect, a perturbed primal.\nTime-step mismatch. The linearised flow has its own Δt_{\\text{lin}}, which need not match Δt_{\\text{primal}}. The tangent and adjoint paths therefore have independent error budgets.\n\nIn practice neither error is fatal for sensitivity analysis — both are of the same order as the discretisation error of the primal itself. They are fatal for the exact-discrete-adjoint use cases above.","title":"What \"continuous\" sacrifices"},{"category":"section","location":"linearised/#Cost-2","page":"Linearised dynamics","text":"Property Value\nDirection Forward (tangent) or backward (adjoint)\nConsistency with primal O(Delta t^d+1) interpolation error\nMemory cost num_steps full-state snapshots in the storage\nForward / backward passes One primal + one linearised pass\nUse when The state is too large for a stage cache\n\nThe memory cost is independent of the scheme's stage count, which is the practical reason this path exists.","title":"Cost"},{"category":"section","location":"linearised/#Trade-offs-in-one-table","page":"Linearised dynamics","text":" Coupled forward Discrete (cache) Continuous (storage)\nForward passes 1 (combined) 1 1\nBackward passes n/a 1 1\nMemory (per primal step) (1 + 1) × scheme stage buf NS × full state 1 × full state\nDiscrete-adjoint identity n/a Exact O(Delta t^d+1) slack\nLinearised Δt = primal Δt = primal Δt independent\nSuitable for adjoint? No Yes Yes\nSuitable for many tangents? Yes (couple them) Yes (replay the cache N×) Yes\nTypical use Sensitivity to a few directions Newton–Krylov, optimisation DNS-scale gradient computation","title":"Trade-offs in one table"},{"category":"section","location":"linearised/#Practical-guidance","page":"Linearised dynamics","text":"A few decision rules that work in practice:\n\nSingle tangent, no adjoint needed. Use the coupled forward path. One integration, no extra memory, exact consistency by construction.\nFew tangents (≤ 5) over short trajectories. Still the coupled path, with the additional perturbations as further coupled components.\nSingle adjoint over a moderate trajectory. Use the discrete (stage cache) path. The memory is bearable, and the discrete-adjoint identity may save you a Newton–Krylov debugging session later.\nSingle adjoint over a very long trajectory or with a huge state. Use the continuous (storage) path with a degree-5 interpolant. The interpolation error is dominated by the time-stepping error in any reasonable setup.\nPeriodic-orbit shooting. Continuous storage path with period = T on the storage; the periodic interpolator wraps endpoints cleanly.\n\nWhen in doubt, prototype with the discrete path on a small problem (where memory is no concern), verify that the gradient passes a finite-difference sanity check, and then switch to the continuous path on the full problem if memory demands it.","title":"Practical guidance"},{"category":"section","location":"linearised/#What-you-write-and-what-the-package-does","page":"Linearised dynamics","text":"Regardless of the path, you write two small pieces of code:\n\na primal right-hand-side, and\na linearised right-hand-side whose signature depends on the path.\n\nPath Linearised RHS signature Primal-state argument\nCoupled forward f_tan(t, x, dxdt, y, dydt) The current x, from the coupled call dependency.\nDiscrete tangent/adj. f_lin(t, x_stage, y, dydt) The cached stage value at the relevant stage time.\nContinuous tangent/adj. f_lin(t, x_interp, y, dydt) The interpolant of the storage at the stage time.\n\nInside, the package handles:\n\nBuilding the time grid (or replaying a cached one).\nCalling the primal interpolant or unpacking stage tuples.\nInverting the time direction for adjoint modes.\nMaintaining allocation-free in-place updates.\n\nYou never write a Jacobian explicitly. You write the action of the Jacobian on a perturbation vector, which is what the linearised right-hand-side does.","title":"What you write and what the package does"},{"category":"section","location":"linearised/#Where-this-is-in-the-code","page":"Linearised dynamics","text":"File What it implements\nsrc/steps/shared.jl NormalMode, ContinuousMode, DiscreteMode; isadjoint(MODE).\nsrc/steps/rk4.jl Three step! overloads for RK4 — normal, continuous, discrete.\nsrc/steps/CNRK2.jl Same three for CNRK2.\nsrc/steps/CB3R2R.jl Three (or four) per scheme for CB3R2R*.\nsrc/integrator.jl The three propagation loops driven by TimeStepFromCache, TimeStepFromStorage, and the constant-step normal loop.\nsrc/stagecache.jl RAMStageCache and its push contract.\nsrc/storage.jl RAMStorage and its Lagrange interpolator.","title":"Where this is in the code"},{"category":"section","location":"linearised/#Worked-examples","page":"Linearised dynamics","text":"The cookbook contains several worked examples that put these paths into practice:\n\nSensitivity to initial conditions of the Lorenz system — coupled forward path.\nLyapunov exponent of the Lorenz attractor — coupled forward + quadrature.\nDiscrete adjoint sensitivity for an optimal-control problem — discrete path.\nContinuous adjoint for a stiff diffusion problem — continuous path.\n\nSee the Cookbook.","title":"Worked examples"},{"category":"section","location":"linearised/#Cross-references","page":"Linearised dynamics","text":"Mathematical foundations — the equations and the adjoint identity.\nTrajectory data — RAMStorage and RAMStageCache details.\nTime stepping — TimeStepFromCache and TimeStepFromStorage policies.\nCoupled systems — the call-dependency machinery used by the coupled forward path.","title":"Cross-references"},{"category":"section","location":"symmetry/#Symmetry-transformations","page":"Symmetry transformations","text":"Many dynamical systems are equivariant under a continuous group action: a Galilean translation, a phase rotation, a spatial shift. The flow of such a system intertwines with the group action, and any algorithm that walks the state space modulo this action benefits from the ability to apply the symmetry as part of the flow. Flows.jl provides exactly this: an optional symmetry-transformation callable attached to every Flows.Flow that is applied (or not) at each call.\n\nThis page explains the math, the API, and the patterns that show up in practice.","title":"Symmetry transformations"},{"category":"section","location":"symmetry/#Equivariance-and-reduction","page":"Symmetry transformations","text":"Let g_s  mathcalXtomathcalX be a one-parameter family of invertible transformations indexed by sinmathbbR. The system is equivariant under g_s if its flow Phi^tau commutes with it:\n\nPhi^tau(g_smathbfx) = g_sPhi^taumathbfxqquadforalltaus\n\nThe canonical example is a translation-invariant PDE: g_s shifts the field by s in the spatial direction, and a primal evolved from a shifted initial condition is the shifted primal.\n\nIn a relative periodic orbit computation, the goal is to find a state mathbfx^star and a pair (T^star s^star) such that\n\nmathbfx^star = g_s^star^-1Phi^T^starmathbfx^star\n\nThe orbit is closed after the action of g_s^star^-1. Numerical schemes that hunt for (mathbfx^star T^star s^star) — Newton on a residual involving g_s^-1Phi^Tmathbfx - mathbfx — need to evaluate g_s^-1Phi^Tmathbfx many times. A flow that does the symmetry application internally is the natural primitive.\n\nA second class of uses is symmetry reduction: post-composing the flow with g_s for a phase s = s(mathbfx) chosen by a \"slice condition\" langlemathbfx boldsymbolnurangle = 0. The reduced flow lives on a slice that intersects each group orbit once, which removes the marginally-stable drift direction.","title":"Equivariance and reduction"},{"category":"section","location":"symmetry/#The-API","page":"Symmetry transformations","text":"A symmetry is supplied as a callable\n\nsym(x, s)        # apply sym parameterised by s to x, in place; return x\n\npassed as the last positional argument to any flow constructor:\n\nF = flow(f, RK4(zeros(3)), TimeStepConstant(0.01), (x, s) -> x[1] += s)\n\nThe constructor wraps the callable: for a single state in a Flows.SymTransform, for a coupled state in a Flows.CoupledTransform. Both collapse to nothing when constructed with nothing, so passing sym=nothing (the default) is equivalent to omitting the argument.\n\nAt the call site, a flow with a non-trivial sym is called with an extra positional argument s:\n\nF(x, (0, T), s)          # apply sym(x, s) to the propagated state before returning\n\nWithout s, the symmetry is not applied:\n\nF(x, (0, T))             # primal advance only\n\nThis is what lets the same F serve as both the raw flow and the symmetry-transformed flow, parameterised by the caller's choice of s.","title":"The API"},{"category":"section","location":"symmetry/#Single-state-vs-coupled-state-symmetries","page":"Symmetry transformations","text":"The two wrappers handle the two state-type cases:\n\nWrapper Applied to Per-component?\nFlows.SymTransform A single state n/a (one call)\nFlows.CoupledTransform A Coupled{N} Yes — one call per component\n\nSymTransform simply delegates: f(x, s) = f.sym(x, s). CoupledTransform loops over the N components and calls f.sym(x[i], s) on each. The implication is that the user's sym callable should be written as if it acted on a single state component, and the wrapper choice is made automatically by flow based on whether the components are coupled.\n\nThe choice of wrapper is made by the appropriate flow overload:\n\nNon-coupled constructors wrap in SymTransform.\nCoupled constructors (those whose g is a Coupled) wrap in CoupledTransform.\n\nYou never instantiate either wrapper yourself in normal use; the public API is just the sym callable.","title":"Single-state vs coupled-state symmetries"},{"category":"section","location":"symmetry/#Patterns","page":"Symmetry transformations","text":"","title":"Patterns"},{"category":"section","location":"symmetry/#Translation-symmetry","page":"Symmetry transformations","text":"sym_shift(x, s) = (x .= circshift(x, round(Int, s)); x)\nF = flow(f, RK4(zeros(N)), TimeStepConstant(Δt), sym_shift)\n\nF(x, (0, T), 10)           # advance, then shift by 10 cells\n\nThe example uses a discrete circshift for simplicity; in spectral codes the same idea is implemented as multiplication by a phase factor in Fourier space.","title":"Translation symmetry"},{"category":"section","location":"symmetry/#Phase-rotation","page":"Symmetry transformations","text":"sym_rotate(x, θ) = (x .= [cos(θ) -sin(θ); sin(θ) cos(θ)] * x; x)\nF = flow(f, RK4(zeros(2)), TimeStepConstant(Δt), sym_rotate)\n\nF(x, (0, T), π/4)          # advance, then rotate by π/4","title":"Phase rotation"},{"category":"section","location":"symmetry/#Slice-reduced-flow","page":"Symmetry transformations","text":"A common pattern is to make sym self-determining — to compute the appropriate s from the state itself, and ignore the caller's s:\n\nfunction sym_slice(x, _ignored)\n    s = best_phase_for_slice(x)\n    apply_phase!(x, s)\n    return x\nend\n\nF = flow(f, RK4(zeros(N)), TimeStepConstant(Δt), sym_slice)\nF(x, (0, T), 0.0)          # `s` is ignored; the slice condition picks the phase\n\nThis is the method-of-slices approach for reducing translational drift from chaotic-attractor visualisations.","title":"Slice-reduced flow"},{"category":"section","location":"symmetry/#Interaction-with-other-constructs","page":"Symmetry transformations","text":"Symmetries compose cleanly with the rest of the package:\n\nMonitors are pushed before the symmetry transformation is applied, so the recorded series is the un-symmetry-reduced trajectory. If you need the reduced trajectory recorded, perform the transformation inside the monitor's observable.\nStorages and stage caches also record the un-reduced trajectory. The symmetry transformation is purely a post-processing step on the final state.\nLinearised flows do not currently apply the symmetry wrapper. If your application needs a symmetry-reduced linearised flow, write the linearised RHS to encode the reduction directly.","title":"Interaction with other constructs"},{"category":"section","location":"symmetry/#Single-application-semantics","page":"Symmetry transformations","text":"The symmetry is applied once, on the final state returned by _propagate!, not at every internal time step. This means the relation\n\nPhi^tau_1+tau_2 = g_s_2Phi^tau_2circg_s_1Phi^tau_1qquad\n(textcompose two symmetry-applied flow calls)\n\nis generally not the same as a single call g_s_1+s_2Phi^tau_1+tau_2 — the symmetry is applied between the two pieces in the composed call but not in the single one. If your algorithm requires the latter behaviour, sum the s values yourself before the single call.","title":"Single-application semantics"},{"category":"section","location":"symmetry/#Cross-references","page":"Symmetry transformations","text":"Architecture — how the symmetry wrapper fits into the four-axis decomposition of Flow.\nCoupled systems — CoupledTransform builds on the Coupled machinery.\nCookbook — symmetry-reduced flow on a 1D PDE.","title":"Cross-references"},{"category":"section","location":"trajectories/#Trajectory-data","page":"Trajectory data","text":"The forward interface of Flows.jl mutates a state in place and returns nothing else. Most algorithms need more: a sampled history of a derived quantity, the full state at intermediate times, or the exact stage values of every accepted step. The package provides three orthogonal data structures that cover these needs:\n\nObject Records Typical use\nMonitor A user-defined observable, one sample per step Time series for plotting / convergence / running averages\nRAMStorage The state (or a derived field) at each accepted step Drives a continuous linearised solve\nRAMStageCache Every internal stage value of every step Drives a discretely consistent linearised solve\n\nA specialised StoreNFromLast monitor records a single observation at a fixed offset before the end of an integration. All four are passed to a Flows.Flow as an extra positional argument, after the state and the time span.\n\nThis page covers each in detail, the relationship between them, and the design choices that put them on this page together.","title":"Trajectory data"},{"category":"section","location":"trajectories/#Monitors","page":"Trajectory data","text":"A Monitor records a single user-defined observable along a trajectory and stores the samples in an in-memory storage. The observable is any callable f(t, x) whose return value is what is actually stored — strings, named tuples, plain numbers, arrays, whatever the user finds useful.","title":"Monitors"},{"category":"section","location":"trajectories/#Construction","page":"Trajectory data","text":"mon = Monitor(x, (t, x)->norm(x))\n\nThe first argument is a template of the system state (used only to infer the type of f(0.0, x) so the backing storage can be preallocated). The second is the observable. There are six keyword arguments:\n\nKeyword Default Purpose\noneevery 1 Decimation. Store one sample every oneevery accepted steps.\nsavebetween (-Inf, Inf) Only store samples whose time falls in this closed interval.\nskipfirst false Drop the very first observation (often the user-supplied initial state).\nsizehint 0 Preallocate capacity in the backing storage.\nio devnull Output stream for the Logger.\nlogevery 1 Decimation of the printed output.","title":"Construction"},{"category":"section","location":"trajectories/#Driving-from-a-flow","page":"Trajectory data","text":"x = rand(3)\nF(x, (0.0, 10.0), mon)\n\ntimes(mon)     # the times at which the observable was sampled\nsamples(mon)   # the corresponding sample values\n\nInside the propagation loop, the schedule is:\n\nOne observation is pushed at the initial time with force=true, before the first step.\nAfter each accepted step the monitor is pushed; the oneevery decimation drops all-but-one-in-oneevery of these, except that the final step is always forced so the recorded series ends at the user-requested endpoint.\nsavebetween and skipfirst are then applied on top.","title":"Driving from a flow"},{"category":"section","location":"trajectories/#Using-monitors-as-callbacks","page":"Trajectory data","text":"The observable does not need to be a pure function. Because it is called every accepted step, it can mutate the state — a useful pattern for re-normalisation or projection onto a constraint:\n\nmon = Monitor(zeros(5), (t, x)->(x ./= norm(x); norm(x)); oneevery=10)\n\nThe mutation is performed before the observable's return value is stored, so the recorded value reflects the post-mutation state.","title":"Using monitors as callbacks"},{"category":"section","location":"trajectories/#StoreNFromLast:-a-specialised-monitor","page":"Trajectory data","text":"A common pattern in optimisation is to need the state a fixed number of steps before the end of an integration: for instance, to seed an adjoint integration from a slightly earlier point so the terminal-condition gradient is well-defined.\n\nStoreNFromLast{N} records exactly one observation, N steps before the final step. It is dispatched by the propagation loop with a separate code path so the decimation logic is exact (no off-by-one errors):\n\nm = StoreNFromLast{2}(zeros(3))      # record state 2 steps before the end\nF(x, (0.0, 1.0), m)\nm.t                                  # the recorded time\nm.x                                  # the recorded state","title":"StoreNFromLast: a specialised monitor"},{"category":"section","location":"trajectories/#Trajectory-printing-with-Logger","page":"Trajectory data","text":"Monitor carries an internal Flows.Logger that pretty-prints the running observable to an io stream. The default is devnull (silent); pass io=stdout to monitor a long run on the terminal.\n\nThe logger introspects the observable's return type once at construction (via the f(0.0, x) it computes for storage typing) and builds a Printf.Format row matching the shape:\n\na single float → one float column,\na tuple of mixed types → one column per element (floats, ints, complex numbers; arrays and other \"unprintable\" entries render as ?).\n\nThe logevery keyword decimates the printed output independently of the oneevery decimation of the stored output, which is useful when you want to see one line per minute of wallclock on a long run but still keep every sample on disk.","title":"Trajectory printing with Logger"},{"category":"section","location":"trajectories/#Storages","page":"Trajectory data","text":"A RAMStorage records the system state at every accepted step and exposes it both as a vector of snapshots and as a callable interpolator. It is the data source for the continuous linearised integration paths.","title":"Storages"},{"category":"section","location":"trajectories/#Construction-2","page":"Trajectory data","text":"store = RAMStorage(x)                       # infer X from `x`\nstore = RAMStorage(typeof(x))               # equivalent\nstore = RAMStorage(x; degree=5, period=2π)  # tuned for a periodic problem\n\nKeyword arguments:\n\nKeyword Default Purpose\nttype Float64 Element type of the time vector.\ndegree 3 Odd Lagrange polynomial degree (≥ 3) used by the interpolator.\nperiod 0.0 Positive period if the data is periodic; zero (default) means non-periodic.\nstorelast true Whether the propagation loop should push the final sample of an integration.","title":"Construction"},{"category":"section","location":"trajectories/#Driving-from-a-flow-2","page":"Trajectory data","text":"x = rand(3)\nF(x, (0.0, 10.0), store)\n\ntimes(store)       # the sampled times\nsamples(store)     # the snapshots\ntimespan(store)    # (first, last) — for periodic storages, (first, period)\n\nA storage is also the data source for the continuous tangent / adjoint integration paths; see Linearised dynamics.","title":"Driving from a flow"},{"category":"section","location":"trajectories/#Interpolation","page":"Trajectory data","text":"A populated storage is callable:\n\nout = similar(samples(store)[1])\nstore(out, t)               # function value at t\nstore(out, t, Val(1))       # first derivative at t (via central differences)\n\nThe interpolation is Lagrange of degree DEG set at construction. The query time t must lie inside timespan(store) — extrapolation throws an ArgumentError. The buffer out must match the snapshot type; the interpolator overwrites it in place and returns it.\n\nThe implementation in src/storage.jl is allocation-free in the hot path:\n\nsearchsortedlast(ts, t) locates the stencil center;\nboundary indices are shifted so the stencil stays inside the data;\nthe Lagrange weights are computed at code-generation time via a @generated helper, so the inner loop is a fixed-length weighted sum.","title":"Interpolation"},{"category":"section","location":"trajectories/#Periodic-storages","page":"Trajectory data","text":"When the recorded data is one period of a periodic signal, set period to the period at construction and do not push the duplicated endpoint. The interpolator then wraps around the endpoints automatically:\n\nstore = RAMStorage(zeros(3); period=2π, storelast=false)\nfor t in range(0, stop=2π, length=N+1)[1:N]\n    push!(store, t, primal_at(t))\nend\nstore(out, 0.0)         # interpolates correctly near the wrap\nstore(out, 2π - 1e-9)   # also fine\n\nThe combination period > 0, storelast = false is the canonical setup for periodic trajectories.","title":"Periodic storages"},{"category":"section","location":"trajectories/#Stage-caches","page":"Trajectory data","text":"A RAMStageCache records the internal stage values of every accepted step. It is the data source for the discretely consistent linearised integration paths.","title":"Stage caches"},{"category":"section","location":"trajectories/#Why-stages-and-not-states?","page":"Trajectory data","text":"The state-at-step-end is what a RAMStorage records. But the discrete tangent equation depends on the value of the operator at exactly the same intermediate stage points as the primal — at t_n + c_kDelta t_n for every stage k, every step n. Recording just the step-end state is not enough; the linearised flow needs each stages[k] and the exact (t_n, Δt_n) that the primal used.\n\nA RAMStageCache{NS} is configured at construction with the stage count NS of the primal scheme and records (t_n, Δt_n, (Y₁,…,Y_{NS})) triples in three parallel vectors:\n\ncache = RAMStageCache(nstages(RK4), zeros(3))     # NS = 4 for RK4\nF(x, (0.0, 1.0), cache)\n\ncache.ts                                          # Vector{Float64}\ncache.Δts                                         # Vector{Float64}\ncache.xs                                          # Vector{NTuple{NS, X}}\n\nThe propagation loop drives the scheme's caching step! overload, which preserves the same time grid as a primal integration without a cache. The only cost is the per-step allocation of the NTuple of stages — there is no extra arithmetic and no extra dispatch overhead.","title":"Why stages and not states?"},{"category":"section","location":"trajectories/#Stage-count-matching","page":"Trajectory data","text":"The propagation loop checks nstages(method) == nstages(cache) at every entry point that takes a cache:\n\ncache = RAMStageCache(2, zeros(3))                # NS = 2\nF     = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))  # NS = 4\nF(x, (0.0, 1.0), cache)                            # ArgumentError\n\nThis catches scheme mismatches at integration time rather than failing silently inside step!.","title":"Stage count matching"},{"category":"section","location":"trajectories/#Driving-a-linearised-flow","page":"Trajectory data","text":"Once a cache is populated, pair it with TimeStepFromCache:\n\nL = flow(f_tangent, RK4(zeros(3), DiscreteMode(false)), TimeStepFromCache())\nL(y, cache)               # marches y through cache.ts forwards\n\nor, for the adjoint mode,\n\nA = flow(f_adjoint, RK4(zeros(3), DiscreteMode(true)), TimeStepFromCache())\nA(q, cache)               # marches q through cache.ts in reverse\n\nSee Linearised dynamics for the full discussion.","title":"Driving a linearised flow"},{"category":"section","location":"trajectories/#Choosing-among-the-three","page":"Trajectory data","text":"The choice between Monitor, RAMStorage, and RAMStageCache is dictated by what the downstream computation needs from the recorded data:\n\nWhat you have What you need to record Use\nA scalar / small observable along a trajectory One value per step Monitor\nThe state at the very end, minus N steps A single state StoreNFromLast\nA driver for a continuous linearised solve State snapshots dense enough to interpolate to the linearised stages RAMStorage\nA driver for a discretely consistent linearised solve All stage values of every step RAMStageCache\n\nAll four can be used simultaneously, in any combination, on the same Flow call: the propagation loop accepts at most one of each, and you can pass them in any order as long as you remain consistent with the call signatures of the Flow method overloads (F(x, span, store), F(x, span, cache), F(x, span, mon), …).","title":"Choosing among the three"},{"category":"section","location":"trajectories/#Cross-references","page":"Trajectory data","text":"Linearised dynamics — full algorithmic context for storages and stage caches.\nTime stepping — TimeStepFromStorage and TimeStepFromCache policies that consume these data structures.\nInternals — Lagrange weight computation and storage memory layout.","title":"Cross-references"},{"category":"section","location":"time-stepping/#Time-stepping","page":"Time stepping","text":"A Flows.Flow is the composition of an integration scheme (one step of the method) and a time-stepping policy (which sequence of (t, Δt) pairs cover the integration interval). The two are orthogonal: any policy works with any scheme. This page catalogues the policies, gives the use case for each, and shows the exact _propagate! body that each policy drives.","title":"Time stepping"},{"category":"section","location":"time-stepping/#Why-the-split","page":"Time stepping","text":"Splitting \"what one step does\" from \"which steps to take\" buys two things:\n\nThe same primal scheme (RK4(zeros(3)), say) can be driven with a constant step, an adaptive step, or — when paired with a stage cache — replayed exactly through a linearised companion. The scheme code is identical in all three cases.\nAdding a new policy does not require modifying any scheme. The schemes implement step!; the policies implement the propagation loop.\n\nThe four shipped policies cover the four common usage patterns in dynamical-systems and PDE work.\n\nPolicy Use\nTimeStepConstant Fixed Δt. The default everyday choice.\nAbstractTimeStepFromHook User supplies a callable that returns the next Δt each step.\nTimeStepFromCache Replay a recorded stage cache. Drives discretely-consistent linearisation.\nTimeStepFromStorage Fixed Δt but interpolate the primal state from a storage. Drives continuous linearisation.","title":"Why the split"},{"category":"section","location":"time-stepping/#Constant-time-step","page":"Time stepping","text":"The everyday default. Marches with a fixed step Δt validated to be strictly positive at construction.\n\nF = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nF(x, (0.0, 1.0))\n\nConstruction-time error checking:\n\nTimeStepConstant(0.0)   # throws: \"time step must be positive\"\nTimeStepConstant(-0.1)  # throws: \"time step must be positive\"\n\nBackward integration is signalled by giving the flow a decreasing span; the sign of Δt is flipped internally:\n\nF(x, (1.0, 0.0))   # marches backwards with the same |Δt|\n\nWhen the requested interval is not a multiple of Δt, the last step is shortened so the endpoint is hit exactly. The mechanism is the Flows.Steps iterator, which is lossy by construction in that case:\n\ncollect(Flows.Steps(0.0, 1.0, 0.3))\n#  →  [(0.0, 0.3), (0.3, 0.3), (0.6, 0.3), (0.9, 0.1)]\n\nThis is the only behaviour that breaks the strict-constant-step invariant. It is benign because the schemes are order-p on every step regardless of Δt, and the truncated step contributes a single extra O(Delta t^p+1) error term to the global error.","title":"Constant time step"},{"category":"section","location":"time-stepping/#Adaptive:-time-step-from-a-user-hook","page":"Time stepping","text":"When the time-step size has to react to the state — e.g. a CFL-driven step in a convection problem — the user supplies a hook: a concrete subtype of AbstractTimeStepFromHook that is callable with signature hook(g, A, z) -> Δt. The flow calls the hook once per step with the current right-hand-side, linear operator, and state, and treats the return value as the next Δt.\n\nstruct CFLHook <: AbstractTimeStepFromHook\n    cmax::Float64    # target CFL number\n    dx::Float64      # grid spacing\nend\n\n(h::CFLHook)(g, A, z) = h.cmax * h.dx / (maximum(abs, z) + eps())\n\nF = flow(f, RK4(zeros(N)), CFLHook(0.7, dx))\nF(x, (0.0, T))\n\nThe hook is expected to return a strictly positive Δt; the propagation loop asserts this. When applying the hook would overshoot the requested endpoint, the step is shrunk so that the endpoint is hit exactly — there is no loss of precision at the boundary.","title":"Adaptive: time step from a user hook"},{"category":"section","location":"time-stepping/#Hook-design-notes","page":"Time stepping","text":"The hook is called before each step. It cannot inspect the state at t + Delta t because that state does not exist yet.\nThe hook receives the explicit and (possibly nothing) implicit operators alongside the state, so it can incorporate operator-dependent estimates of the timescale without keeping a reference to the system itself.\nA hook with state of its own (a step controller, a PID, ...) is best implemented as a mutable struct so its state can evolve between calls.","title":"Hook design notes"},{"category":"section","location":"time-stepping/#Replay:-time-step-from-a-stage-cache","page":"Time stepping","text":"A populated RAMStageCache records the exact (t Delta t mathrmstages) triples produced by a primal integration. The TimeStepFromCache policy is a singleton (no Δt parameter) that drives a linearised flow through exactly the same time grid, in order. This is the mechanism behind discretely-consistent tangent and adjoint integration.\n\n# primal: cache the stages\ncache = RAMStageCache(nstages(RK4), zeros(3))\nF     = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nF(x, (0.0, 1.0), cache)\n\n# tangent: replay the cache\nL = flow(f_tangent, RK4(zeros(3), DiscreteMode(false)), TimeStepFromCache())\nL(y, cache)                    # no `span`: the cache contains it\n\nThe propagation loop walks cache.ts[i], cache.Δts[i], cache.xs[i] either forward (tangent) or in reverse (adjoint) depending on the mode tag carried by the scheme.","title":"Replay: time step from a stage cache"},{"category":"section","location":"time-stepping/#When-to-use-it","page":"Time stepping","text":"TimeStepFromCache is the right answer whenever a discretely-consistent tangent or adjoint is required. The cookbook Discrete adjoint sensitivity in the Cookbook walks through a complete example. See Linearised dynamics for the comparison with the continuous alternative.","title":"When to use it"},{"category":"section","location":"time-stepping/#Replay:-time-step-from-a-storage","page":"Time stepping","text":"The continuous counterpart. A primal trajectory is recorded into a RAMStorage at the times of the primal integration, and a linearised flow marches with its own fixed Δt, interpolating the primal trajectory at every internal-stage time it needs. The interpolation uses a Lagrange polynomial of the degree configured on the storage.\n\n# primal: record into a storage\nstore = RAMStorage(zeros(3); degree=5)\nF     = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nF(x, (0.0, 1.0), store)\n\n# tangent / adjoint: continuous integration\nL = flow(f_lin, RK4(zeros(3), ContinuousMode(false)), TimeStepFromStorage(0.005))\nL(y, store, (0.0, 1.0))\n\nTwo differences from TimeStepFromCache are worth highlighting:\n\nThe linearised flow chooses its own Δt. It does not have to match the primal grid. A common pattern is to record on a coarse grid and integrate the linearisation on a finer one.\nThe result is not discretely consistent: the linearisation evaluates the operator at an interpolant of the primal, not at the primal's exact stage values. See Linearised dynamics for what this means in practice.\n\nThe constructor validates Δt > 0 like TimeStepConstant. Backward integration is again signalled by a decreasing span.","title":"Replay: time step from a storage"},{"category":"section","location":"time-stepping/#Putting-it-together","page":"Time stepping","text":"The four policies cover four positions in a \"discretely-consistent ⟷ continuous\" times \"primal ⟷ adaptive\" grid:\n\n Fixed step Adaptive step Replay from a record\nPrimal TimeStepConstant AbstractTimeStepFromHook —\nLinearised, continuous TimeStepFromStorage — —\nLinearised, discrete — — TimeStepFromCache\n\nAdaptive linearisation and from-cache primal are not provided because they have no natural use in the algorithms Flows.jl targets.","title":"Putting it together"},{"category":"section","location":"time-stepping/#Cross-references","page":"Time stepping","text":"Trajectory data — storages and stage caches, the data sources for the replay policies.\nLinearised dynamics — the algorithmic context that the replay policies serve.\nInternals — the _propagate! overloads, one per policy.","title":"Cross-references"},{"category":"section","location":"coupled/#Coupled-systems","page":"Coupled systems","text":"Many problems are not one differential equation in one state but a small number of equations in heterogeneous states that need to advance together. Common examples are\n\na primal system plus a tangent (linearised) perturbation,\na primal system plus an adjoint state,\na primal system plus several trajectory integrals,\na primal system plus several independent tangent perturbations.\n\nFlows.jl supports these natively via the Coupled state type and the CallDependency interface. This page explains the motivation, the type, the broadcasting machinery, and the construction of coupled flows.","title":"Coupled systems"},{"category":"section","location":"coupled/#Motivation","page":"Coupled systems","text":"Let mathbfx(t)inmathcalX and mathbfy(t)inmathcalY be two state components governed by\n\nleft\nbeginaligned\ndotmathbfx(t) = mathbff(tmathbfx(t))\ndotmathbfy(t) = mathbfg(tmathbfx(t)mathbfy(t))\nendaligned\nright\n\nThe dynamics of mathbfx is autonomous in mathbfx alone; the dynamics of mathbfy depends on both mathbfx and mathbfy.\n\nA naïve strategy is to solve the first equation alone, store its solution at some grid of times, and then drive the second equation with a stored or interpolated trajectory. For very large state spaces (DNS of turbulent flows is the canonical example) storing the entire mathbfx-trajectory is prohibitive: mathbfx can be tens of GB per snapshot. The alternative is to advance both components together, with the integration scheme treating the composite mathbfz(t) = (mathbfx(t)mathbfy(t)) as a single state.\n\nFlows.jl's solution is a thin wrapper type, Coupled, that holds the components as an NTuple and forwards every package-relevant operation (broadcast, copy, similar, indexing) to them.","title":"Motivation"},{"category":"section","location":"coupled/#The-Coupled-type","page":"Coupled systems","text":"A Coupled{N, ARGS} wraps an NTuple{N, Any}. It is constructed with couple:\n\njulia> z = couple([1.0, 2.0, 3.0], [0.0, 4.0])\n2-element Coupled{2, Tuple{Vector{Float64}, Vector{Float64}}}:\n [1.0, 2.0, 3.0]\n [0.0, 4.0]\n\njulia> z[1]\n3-element Vector{Float64}:\n 1.0\n 2.0\n 3.0\n\njulia> length(z)\n2\n\nThe wrapper is itself immutable; mutating the components of z is fine, but z[2] = ... is not. This invariant lets the schemes treat a Coupled state as if it were a fixed-size container.","title":"The Coupled type"},{"category":"section","location":"coupled/#Broadcasting","page":"Coupled systems","text":"The key property of Coupled is that it participates in Julia's dot-broadcasting machinery. The expression\n\nz .= 2 .* z .+ y\n\nis interpreted as\n\nz[1] .= 2 .* z[1] .+ y[1]\nz[2] .= 2 .* z[2] .+ y[2]\n\nwithout any temporary Coupled ever being allocated. The mechanism is a custom Broadcast.BroadcastStyle and a @generated copyto! that unfolds the N per-component broadcasts at compile time. Numbers in the expression pass through unchanged, so mixing scalars and Coupleds in a single fused dot expression is supported.\n\nThis is what makes coupled-system integration in Flows.jl competitive with hand-written single-system code: every step! body of every scheme is written in terms of dot expressions, and the same body works for N = 1, 2, 3, …. There is no per-step tuple unpacking at run time.","title":"Broadcasting"},{"category":"section","location":"coupled/#Independence-of-components","page":"Coupled systems","text":"couplecopy(N, x) is a convenience for couple(deepcopy(x), deepcopy(x), …). The components are guaranteed to be independent (no shared storage):\n\nz = couplecopy(3, zeros(5))\nz[1][1] = -1\n@assert z[2][1] == 0      # other components are untouched\n\nThis is useful for, e.g., evolving several independent tangent perturbations of the same primal in lock-step.","title":"Independence of components"},{"category":"section","location":"coupled/#Constructing-a-coupled-flow","page":"Coupled systems","text":"Building a Flows.Flow on a coupled state is mechanically identical to the single-state case, with two changes:\n\nThe right-hand-side is a Coupled{N} of callables, one per component.\nThe integration scheme is constructed with a Coupled{N} template state, so its internal buffers are themselves Coupled{N}.\n\nf(t, x, dxdt)              = …            # primal RHS\ng(t, x, dxdt, y, dydt)     = …            # second-component RHS\n\nx = zeros(3)\ny = zeros(3)\n\nF = flow(couple(f, g),\n         RK4(couple(x, y)),\n         TimeStepConstant(0.01))\n\nF(couple(x, y), (0.0, 1.0))\n\nThe signature of g here is the default one: each component sees itself and every preceding component. This is the right signature for tangent equations, quadrature equations, and most other \"primal + extension\" workflows.","title":"Constructing a coupled flow"},{"category":"section","location":"coupled/#Default-call-dependency","page":"Coupled systems","text":"The default expansion of a coupled right-hand-side call is\n\ng[1](t,  u1, du1dt)\ng[2](t,  u1, du1dt,  u2, du2dt)\ng[3](t,  u1, du1dt,  u2, du2dt,  u3, du3dt)\ng[4](t,  u1, du1dt,  u2, du2dt,  u3, du3dt,  u4, du4dt)\n\ni.e. component i depends on components 1 2 dots i. The default is constructed by default_dep(N) and is set up for N ∈ 1:3 directly; larger N is supported via the explicit construction below.","title":"Default call dependency"},{"category":"section","location":"coupled/#Custom-call-dependencies","page":"Coupled systems","text":"Some coupling patterns do not fit the default. Two independent tangent perturbations mathbfy_1 mathbfy_2 of the same primal mathbfx obey\n\nleft\nbeginaligned\ndotmathbfx(t)   = mathbff(tmathbfx(t))\ndotmathbfy_1(t) = mathbfg_1(tmathbfx(t)mathbfy_1(t))\ndotmathbfy_2(t) = mathbfg_2(tmathbfx(t)mathbfy_2(t))\nendaligned\nright\n\nNote that mathbfg_1 does not depend on mathbfy_2, and vice versa. The default dependency would force the signature of mathbfg_2 to receive y1 whether or not it uses it. This is wasteful, and it is also misleading: the right-hand-side declares dependencies it does not have.\n\nThe fix is a CallDependency:\n\ndeps = CallDependency((1,),     # component 1 depends on itself\n                      (1, 2),   # component 2 depends on 1 and itself\n                      (1, 3))   # component 3 depends on 1 and itself\n\nF = flow(couple(f, g1, g2),\n         deps,\n         RK4(couple(zeros(3), zeros(3), zeros(3))),\n         TimeStepConstant(0.01))\n\nwith\n\nf(t,  x, dxdt)\ng1(t, x, dxdt,  y1, dy1dt)\ng2(t, x, dxdt,  y2, dy2dt)\n\nCallDependency((spec1,), (spec2,), …) accepts N tuples of positive integers; each tuple spec_i is the sorted list of component indices that flow into the signature of component i. The constructor enforces:\n\nevery index lies in 1:N,\nevery spec is sorted in increasing order,\nthe inner tuples carry no duplicates (sorted strict-monotone).","title":"Custom call dependencies"},{"category":"section","location":"coupled/#Coupled-IMEX-systems","page":"Coupled systems","text":"For IMEX problems the linear part is also a Coupled{N}. Each component may be either a linear operator (with ImcA! and mul!) or nothing. The nothing marker means advance this component fully explicitly — the universal idiom for a quadrature component appended to a stiff primal:\n\n# stiff primal + explicitly-advanced quadrature\nf_ex(t, x, dxdt) = …\nquad(t, x, dxdt, I, dIdt) = (dIdt[1] = x[1]^2)\n\nL = Diagonal(...)        # linear operator for the primal\n\nF = flow(couple(f_ex, quad),\n         couple(L, nothing),\n         CNRK2(couple(zeros(3), zeros(1))),\n         TimeStepConstant(0.01))\n\nThe IMEX step! implementations check the type of each component of A: when A[i] is nothing, the implicit subproblem for component i collapses to z .= y (the identity), and the explicit part takes the whole step. When A[i] is non-nothing, the full IMEX update is applied. The dispatch is done at compile time via @generated functions in src/system.jl, so the nothing-branch costs nothing at run time.","title":"Coupled IMEX systems"},{"category":"section","location":"coupled/#What-Coupled-is-*not*","page":"Coupled systems","text":"A few non-uses worth flagging:\n\nIt is not for parallelism. Each component still advances on the same thread.\nIt is not a general \"tuple of states\" abstraction. The schemes' broadcast rules assume the components share the same shape of broadcast operations. Mixing wildly different state types in one Coupled (a Vector{Float64} and a SparseMatrixCSC, say) may work, but is outside the design envelope.\nIt is not nested. Coupled of Coupled is not supported in the broadcasting code paths and is not tested.","title":"What Coupled is not"},{"category":"section","location":"coupled/#Cross-references","page":"Coupled systems","text":"States and vector fields — single-component requirements.\nQuadrature equations — the canonical \"primal + extension\" coupled use case.\nLinearised dynamics — coupled primal+tangent for forward-mode sensitivity.\nInternals — the @generated broadcast machinery in src/couple.jl.","title":"Cross-references"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"This page describes how Flows.jl is put together: which types own which responsibility, how they fit into one another, and how F(x, span) becomes a sequence of step! calls. It is the right page to read once if you intend to extend the package, write a custom scheme, or debug performance.","title":"Architecture"},{"category":"section","location":"architecture/#The-flow-as-a-composition","page":"Architecture","text":"A Flow is a four-field container:\n\nmutable struct Flow{TS, M, S, SO}\n    tstep::TS   # ::AbstractTimeStepping  — how the time grid is built\n    meth ::M    # ::AbstractMethod        — one-step scheme + preallocated stages\n    sys  ::S    # ::System                — vector field(s) and optional L\n    sym  ::SO   # ::Nothing or SymTransform or CoupledTransform\nend\n\nThe four fields are orthogonal in the sense that they can be combined freely:\n\nFlow ≅ TimeStepping  ⊗  Method  ⊗  System  ⊗  Symmetry\n\nAxis Concrete types What it decides\nTime stepping TimeStepConstant, AbstractTimeStepFromHook, TimeStepFromCache, TimeStepFromStorage The sequence of (t_n Delta t_n) pairs\nMethod RK4, CNRK2, CB3R2R2, CB3R2R3e, CB3R2R3c, CB4R3R4 What one step does, and how many stage buffers it owns\nSystem System{N, DEPS, GT, AT} Which right-hand-side(s) and optional linear operator(s) are evaluated\nSymmetry Nothing, SymTransform, CoupledTransform Post-step transformation applied when the user supplies s\n\nThe construction entry point is the flow factory; it has many overloads only because the constructor signature changes when more axes are present. The internal representation never branches on what the user passed.","title":"The flow as a composition"},{"category":"section","location":"architecture/#Layered-call","page":"Architecture","text":"Calling a flow walks a fixed pipeline:\n\nF(x, (t0, T))\n   └─→ _propagate!(meth, tstep, sys, span, x, cache?, store?, mon?)\n            └─→ for (t, Δt) in Steps(t0, T, tstep.Δt)\n                    step!(meth, sys, t, Δt, x, cache?)\n                    (push! into mon? / store?)\n            return x\n\nThree observations:\n\nThe time iterator is a value, not a callback. Flows.Steps is a thin AbstractVector over a Julia Range; its iteration produces (t, Δt) pairs without any per-iteration heap allocation. The final pair may carry a shorter Δt when the span is not a multiple of the nominal step.\nstep! is dispatched on (method, mode). Every scheme implements several step! methods, one per MODE it supports: the normal-mode one runs the primal, the ContinuousMode ones interpolate from a storage, and the DiscreteMode ones replay a tuple of stages. Adding a scheme means writing a new struct plus one step! per mode.\nMonitors, storages, and caches are dispatch-time options. _propagate! is parameterised over the types of mon, store, and cache; when any of them is Nothing, the corresponding branch is dead code at the LLVM level. There is no runtime \"if monitor != nothing\" check.","title":"Layered call"},{"category":"section","location":"architecture/#The-System-wrapper","page":"Architecture","text":"A Flows.System bundles the right-hand-side(s) g and the optional linear operator(s) A. The single-component version just stores (g, A); the multi-component version stores tuples plus a CallDependency describing which components flow into which signature.\n\nSystem is callable. For the explicit part it walks DEPS at code-generation time and emits a single call to each component:\n\n@generated function (sys::System{N, DEPS})(t, z::Coupled{N}, dzdt::Coupled{N}) where {N, DEPS}\n    expr = quote end\n    for i = 1:N\n        # build the tuple (z[d], dzdt[d]) for each d in DEPS[i]\n        push!(expr.args, :( sys.g[$(Val(i))](t, <unpacked>...) ))\n    end\n    return expr\nend\n\nThe @generated body sees N and DEPS as compile-time constants, so the resulting code is a flat sequence of calls with no runtime tuple manipulation in the hot path.\n\nFor the implicit part, System provides three thin shims:\n\nmul!(out, sys, z)        # action of A on z; falls back to (out .= 0) when A === nothing\nImcA!(sys, c, y, z)      # solve (I - cA) z = y;     falls back to z .= y when A === nothing\nImcA_mul!(sys, c, y, z)  # compute (I - cA) y;       falls back to z .= y when A === nothing\n\nThe nothing fallback is what makes the quadrature-via-coupling idiom work: an explicitly-marched quadrature component pairs the user's f with nothing for A, and the IMEX scheme sees the quadrature row as if it were a no-op implicit problem.","title":"The System wrapper"},{"category":"section","location":"architecture/#The-integration-method","page":"Architecture","text":"Every concrete scheme is a struct ConcreteMethod{X, MODE, NX} <: AbstractMethod{X, MODE, NS}:\n\nType parameter Role\nX The state type. Locked at construction; passing a state of a different type is an error.\nMODE The integration mode tag (see below). Locks which step! is dispatched.\nNS The stage count. Matched against any AbstractStageCache passed at integration time.\n\nThe scheme stores all preallocated buffers in a single NTuple{N, X} field named store, where N depends on the scheme and on the mode: a ContinuousMode RK4 needs an extra buffer to hold the interpolant compared with a NormalMode one. See Integration schemes for the exact counts.","title":"The integration method"},{"category":"section","location":"architecture/#Mode-tags","page":"Architecture","text":"The mode is a zero-cost type tag — an empty struct whose only job is to pick a code path.\n\nTag When\nNormalMode Forward nonlinear (primal) integration\nContinuousMode(false) / (true) Continuous tangent / adjoint, driven by a RAMStorage\nDiscreteMode(false) / (true) Discretely consistent tangent / adjoint, driven by a RAMStageCache\n\nisadjoint(MODE) returns true only for the *Mode(true) variants and is the predicate the schemes use to decide whether to march forwards or backwards.","title":"Mode tags"},{"category":"section","location":"architecture/#Time-stepping-policies","page":"Architecture","text":"The three propagation paths in src/integrator.jl are selected by the (method, tstep) pair:\n\ntstep type _propagate! body\nTimeStepConstant walks Steps(t0, T, Δt) and calls step!(method, sys, t, Δt, z, cache)\n<: AbstractTimeStepFromHook repeatedly queries hook(g, A, z) for the next Δt until t == T\nTimeStepFromCache replays (ts[i], Δts[i], xs[i]) from a stage cache, no span argument\nTimeStepFromStorage walks Steps(t0, T, Δt) but threads the storage into step!\n\nThe first three are for primal integration; TimeStepFromCache and TimeStepFromStorage are the entry points used by linearised flows.","title":"Time-stepping policies"},{"category":"section","location":"architecture/#Coupling:-the-Coupled-type","page":"Architecture","text":"A Coupled{N, ARGS} is a thin wrapper around an NTuple{N, Any}. It indexes (z[i]), it has length N, and — critically — it participates in Julia's broadcasting infrastructure: a dot expression involving Coupleds and Numbers is forwarded down to the components, so the time-stepping kernels do\n\ny .= x .+ Δt .* k1\n\nuniformly regardless of whether x is a single state or a coupled state.\n\nThe broadcast machinery (Base.copyto!(::Coupled{N}, ::Broadcasted{CoupledStyle})) is @generated so that, for each statically-known N, it expands to exactly N calls into per-component copyto! with no tuple iteration at run time. The implementation pattern follows MultiScaleArrays.jl.","title":"Coupling: the Coupled type"},{"category":"section","location":"architecture/#Generated-dispatch-on-N","page":"Architecture","text":"Because the broadcast happens through @generated, the same step! body works for N=1, 2, 3, …. The cost of supporting many N is paid once, at compile time, for each N the user actually uses. This is the reason coupled-system integration in Flows.jl is as fast as hand-written single-system code: there is no per-step tuple unpacking.","title":"Generated dispatch on N"},{"category":"section","location":"architecture/#The-symmetry-wrapper","page":"Architecture","text":"After _propagate! returns, the optional sym field of the flow is applied if and only if the user supplied a third positional argument s:\n\nF(x, span)              # _propagate!(...); return x\nF(x, span, s)           # I.sym(_propagate!(...), s); return x\n\nBoth Flows.SymTransform and Flows.CoupledTransform are themselves callables. The wrapper exists so that the Flow carries a uniform type discipline: F.sym is either nothing, or a callable that maps a state and a parameter to the transformed state. The dispatch on SO <: NoTransform (where NoTransform == Nothing) is what suppresses the wrapping in the no-symmetry case at zero cost.","title":"The symmetry wrapper"},{"category":"section","location":"architecture/#File-map","page":"Architecture","text":"A direct correspondence between the abstractions and the source layout:\n\nConcept File(s)\nCoupled, broadcasting, sym wrappers src/couple.jl\nButcher tableaux + CB*_I/E constants src/tableaux.jl\nSteps time iterator src/stepper.jl\nSystem, CallDependency src/system.jl\nRAMStorage, Lagrange interpolation src/storage.jl\nRAMStageCache src/stagecache.jl\nAbstractMethod, mode tags src/steps/shared.jl\nSchemes (one file per scheme) src/steps/{rk4,CNRK2,CB3R2R,CB4R3R}.jl\n_propagate!, Flow callable surface src/integrator.jl\nMonitor, Logger src/monitor.jl, src/logger.jl\nStoreNFromLast specialised monitor src/storenfromlast.jl\nTimeStepConstant, hook, cache, storage src/timestepping.jl\nImcA!, ImcA_mul!, Diagonal fallback src/imca.jl\nStand-alone quadrature rules src/utils.jl\n\nThe dependency graph is roughly: couple.jl → tableaux.jl → stagecache.jl → system.jl → storage.jl → steps/* → timestepping.jl → integrator.jl. The top-level Flows.jl includes them in that order.","title":"File map"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"This page documents the implementation of Flows.jl: data layouts, generated code, dispatch tricks, and design rationale. It is intended for contributors, users debugging a performance regression, and curious readers who already understand the user-facing API. The conceptual map of the package is on Architecture; this page goes one level deeper.","title":"Internals"},{"category":"section","location":"internals/#Module-layout","page":"Internals","text":"Flows.jl is a single module split across one source file per concern. Files are included in dependency order from src/Flows.jl:\n\ncouple.jl  →  tableaux.jl  →  stagecache.jl  →  system.jl  →  storage.jl\n   ↓\nsteps/shared.jl  →  steps/{rk4,CNRK2,CB3R2R,CB4R3R}.jl\n   ↓\ntimestepping.jl  →  logger.jl  →  monitor.jl  →  storenfromlast.jl  →  imca.jl\n   ↓\nstepper.jl  →  integrator.jl  →  utils.jl\n\nEach file holds one self-contained piece of functionality. There is no cyclic dependency. The flat module structure is intentional: the package is small enough that submodules would add navigation overhead without buying any encapsulation.","title":"Module layout"},{"category":"section","location":"internals/#The-Coupled-broadcast-machinery","page":"Internals","text":"src/couple.jl defines a CoupledStyle <: Broadcast.ArrayStyle{Coupled} and a single @generated Base.copyto!. The generated body unfolds, at compile time, a per-component copyto! for each of the N slots:\n\n@generated function Base.copyto!(dest::Coupled{N},\n                                   bc::Broadcast.Broadcasted{CoupledStyle}) where {N}\n    quote\n        $(Expr(:meta, :inline))\n        Base.Cartesian.@nexprs $N i -> begin\n            @inbounds copyto!(getfield(dest.args, i), unpack(bc, Val(i)))\n        end\n        return dest\n    end\nend\n\nunpack(bc, Val(i)) walks the Broadcasted tree and replaces every Coupled operand by its i-th component, leaving Numbers and other operands untouched. The result is a per-component Broadcasted that copyto! can dispatch on the underlying component type — which usually falls through to the default AbstractArray broadcast.\n\nThe pattern is adapted from MultiScaleArrays.jl. It is the reason a single hand-written step! body like\n\ny .= x .+ Δt .* k1\n\nworks uniformly for any N, and that the generated code has zero per-step tuple-iteration overhead.","title":"The Coupled broadcast machinery"},{"category":"section","location":"internals/#Tableau-storage","page":"Internals","text":"src/tableaux.jl carries the Butcher tableaux as Tableau and IMEXTableau structs. The four IMEX tableaux from Cavaglieri & Bewley (2015) are entered as exact Rational arrays and converted once, at module load, to Float64:\n\nconst CB2 = convert(IMEXTableau{Float64, 3}, IMEXTableau(CB2_I, CB2_E))\n\ngetindex(tab, :aᴵ, i, j) and friends return coefficients by their mathematical role. The unicode superscripts make the IMEX bookkeeping in src/steps/CB*.jl line up visually with the paper.\n\nThe tableau types support convert(::Type{Tableau{T, NS}}, tab), so a tableau in any element type (rationals, Float32, Float64) can be coerced to whatever the schemes need.","title":"Tableau storage"},{"category":"section","location":"internals/#Steps:-the-time-iterator","page":"Internals","text":"src/stepper.jl defines\n\nstruct Steps{S, R<:AbstractRange{S}} <: AbstractVector{Tuple{S, S}}\n        rng::R\n          T::S\n    isLossy::Bool\nend\n\n— a thin AbstractVector wrapping a Julia Range, with a flag for whether the range covers the requested span exactly. Iteration produces (t, Δt) pairs:\n\nFor the lossless case (last(rng) == T), Δt is constant.\nFor the lossy case, the final Δt is shortened to T - last(rng) so that t + dt == T exactly.\n\nThe constructor accepts a positive dt and flips its sign internally when t0 > T so that backward integration shares the same iteration code path.\n\nThere is no heap allocation anywhere in Steps. Iteration is just two integer-index lookups into a Range. This is why the propagation loops in src/integrator.jl are allocation-free.","title":"Steps: the time iterator"},{"category":"section","location":"internals/#System:-generated-dispatch-on-call-dependencies","page":"Internals","text":"src/system.jl defines System{N, DEPS, GT, AT} whose call operator is @generated on (N, DEPS). The body builds a flat sequence of calls into the components:\n\n@generated function (sys::System{N, DEPS})(t, z::Coupled{N}, dzdt::Coupled{N}) where {N, DEPS}\n    expr = quote end\n    for i = 1:N\n        tup = Expr(:tuple)\n        for d in DEPS[i]\n            append!(tup.args, (:(z[$d]), :(dzdt[$d])))\n        end\n        push!(expr.args, :(sys.g[$(Val(i))](t, $(tup)...)))\n    end\n    return expr\nend\n\nDEPS is a CallDependency{N, INFO} whose INFO is a compile-time tuple-of-tuples. The @generated walks INFO and emits exactly the right call signature for each component. The result, after specialisation, is a straight-line sequence of calls — no tuple iteration, no runtime dispatch on DEPS.\n\nThe same idea is used for the implicit-part bookkeeping: mul!(out, sys, z) and ImcA!(sys, c, y, z) are @generated on AT, walking the type parameters and emitting (out[i] .= 0) or (z[i] .= y[i]) whenever the matching A[i] is Nothing. The nothing branch costs nothing at run time.","title":"System: generated dispatch on call dependencies"},{"category":"section","location":"internals/#Mode-tags","page":"Internals","text":"src/steps/shared.jl defines NormalMode, ContinuousMode{ISADJOINT}, and DiscreteMode{ISADJOINT}. They are empty structs whose only role is to dispatch the appropriate step! overload of each scheme.\n\nisadjoint(::Type{NormalMode}) = false and isadjoint(::Type{*Mode{ISADJOINT}}) = ISADJOINT are the predicates used inside the schemes to flip a sign on Δt (and to choose whether to iterate the stage cache forwards or backwards in src/integrator.jl).","title":"Mode tags"},{"category":"section","location":"internals/#Integration-schemes","page":"Internals","text":"Every scheme follows the same layout:\n\nstruct SCHEME{X, MODE, NX} <: AbstractMethod{X, MODE, NS}\n    store::NX     # NTuple{N, X} of preallocated stage buffers\nend\n\nwith one outer constructor that takes a template x::X and a mode::AbstractMode, chooses N based on the mode, and allocates ntuple(i -> similar(x), N).\n\nThe user-facing step!(method, sys, t, Δt, x, cache_or_stages) is implemented as several overloads, one per MODE:\n\nNormalMode — primal forward step with optional caching of internal stages.\nContinuousMode{ISADJOINT} — interpolated-primal step, sign of Δt determined by ISADJOINT.\nDiscreteMode{ISADJOINT} — replay-of-cached-stages step, forwards if false, backwards-and-transposed if true.\n\nThe CB schemes additionally use @eval-driven code generation in src/steps/CB3R2R.jl to produce three concrete types (CB3R2R2, CB3R2R3e, CB3R2R3c) from a single shared step! template parameterised on the tableau.","title":"Integration schemes"},{"category":"section","location":"internals/#Propagation-loops","page":"Internals","text":"src/integrator.jl contains the four propagation kernels:\n\n_propagate! overload Used by\n(method::AbstractMethod{Z, NormalMode}, stepping::TimeStepConstant, ...) All primal flows with constant time step\n(method::AbstractMethod{Z, MODE<:NormalMode}, hook::AbstractTimeStepFromHook, ...) Hook-driven primal flows\n(method::AbstractMethod{Z, MODE<:DiscreteMode}, system, z, cache, mon) Discrete tangent and adjoint\n(method::AbstractMethod{Z, MODE<:ContinuousMode}, stepping::TimeStepFromStorage, ...) Continuous tangent and adjoint\n\nEach loop walks a Steps iterator (or the cache's recorded time grid) and calls the scheme's step!. The cache, storage, and monitor branches are dispatched on the static types of the corresponding arguments, so _propagate! is fully specialised on each combination — no per-step isa check.","title":"Propagation loops"},{"category":"section","location":"internals/#RAMStorage-and-Lagrange-interpolation","page":"Internals","text":"src/storage.jl stores times and snapshots in parallel Vectors. The interpolator is callable:\n\n(store::RAMStorage{T, X, DEG})(out::X, t::Real, ::Val{ORD}=Val(0))\n\nThree steps:\n\nStencil selection. _interp_indices does a searchsortedlast, then shifts the stencil if necessary to keep it inside the data (non-periodic case) or to wrap it modulo the length (periodic case). The shift logic is unrolled at code-generation time by being inside a @generated function with Val(N).\nPeriodic-wrap adjustment. _make_tuple_of_times constructs a strictly increasing tuple of times by adding period to entries that follow the wrap point, and adjusts the query time t if it falls outside the new interval.\nLagrange weights and weighted sum. _lagr_weights is generated for each N and produces an NTuple{N, Float64} of weights via _prod(t, ts, Val(j)). _lagr_interp then walks the indices and accumulates the result in place into out.\n\nThe weighted-sum loop is out .= ws[1] .* xs[rng[1]]; for i in 2:N; out .+= ws[i] .* xs[rng[i]]; end — fully dot-broadcast, allocation-free, and dispatched on the component type via the standard AbstractArray broadcast machinery.\n\nThe first-derivative variant _lagr_weights(t, ts, Val(1)) is a four-point central-difference approximation of the function-value weights. This is a stop-gap until an analytic derivative is implemented; the current accuracy is sufficient for the few use cases that need a derivative interpolant.","title":"RAMStorage and Lagrange interpolation"},{"category":"section","location":"internals/#RAMStageCache","page":"Internals","text":"src/stagecache.jl is intentionally minimalist: three parallel Vectors for (ts, Δts, xs), with push! taking the three pieces and appending in lock-step. The stage tuple xs[i] :: NTuple{NS, X} is pushed as a whole tuple per step, so the storage layout matches what the replay path consumes.\n\nnstages(::AbstractStageCache{NS}) = NS is the predicate used by every propagation loop that takes a cache, to catch scheme/cache mismatches at the entry point.","title":"RAMStageCache"},{"category":"section","location":"internals/#Monitor-and-Logger","page":"Internals","text":"src/monitor.jl implements a mutable struct Monitor that delegates storage to any AbstractStorage. The push policy is implemented in a single Base.push!(mon, t, x, force):\n\nif force || (mon.count % mon.oneevery == 0)\n    if isbetween(t, mon.savebetween...)\n        if !(mon.count == 0 && mon.skipfirst)\n            push!(mon.store, t, mon.f(t, x))\n            …                                  # printf via Logger\n        end\n    end\nend\nmon.count += 1\n\nforce is the integration loop's mechanism for guaranteeing that the final step is always pushed, regardless of oneevery decimation.\n\nThe optional Logger in src/logger.jl is a mutable struct Logger{N, O, F} whose call operator prints either a header or a row, depending on whether it is called with no arguments or with (count, store). The Printf.Format row is built once at construction by inspecting the type of the observable: floats, integers, and complex numbers each get their own format width; everything else renders as ?.","title":"Monitor and Logger"},{"category":"section","location":"internals/#Symmetry-wrappers","page":"Internals","text":"src/couple.jl defines SymTransform{SO} and CoupledTransform{SO}, both callables. Their constructors collapse to nothing when given nothing, so the no symmetry case is represented by a uniform Nothing — const NoTransform = Nothing.\n\nThe Flow callable dispatches on SO <: NoTransform to elide the sym(...) call when there is no symmetry, keeping the two-argument call F(x, span) as cheap as a flow built without a symmetry.","title":"Symmetry wrappers"},{"category":"section","location":"internals/#Allocation-discipline","page":"Internals","text":"The package's test suite contains @allocated regression tests at the boundary of every propagation path:\n\ntest/test_integrator.jl checks that F(x, span) allocates zero bytes after warmup.\ntest/test_steps.jl checks that Flows.step!(scheme, sys, t, Δt, x, nothing) allocates zero bytes.\ntest/test_quadrature.jl checks that the coupled-quadrature path is allocation-free.\n\nTwo known offenders are tagged @test_broken:\n\nThe Logger-attached Monitor allocates once per push because the Printf.format machinery allocates a String.\nThe CB3R2R step! cache branch allocates a temporary X[] vector that is then tuple(...)-ed before being pushed. This is a known TODO in the code.\n\nThese regressions are bounded and localised; they do not affect the hot path of a primal integration with no monitor.","title":"Allocation discipline"},{"category":"section","location":"internals/#Where-to-start-when-contributing","page":"Internals","text":"If you want to extend the package, the entry points are:\n\nTo add Touch\nA new scheme src/steps/yourscheme.jl (plus an include in src/Flows.jl); add at least step! for NormalMode. Add ContinuousMode and DiscreteMode for linearised support.\nA new linear-operator type Write LinearAlgebra.mul! and Flows.ImcA! for it. No Flows change needed.\nA new time-stepping policy Add a subtype of AbstractTimeStepping (or AbstractTimeStepFromHook) and a _propagate! overload that takes it.\nA new storage backend Subtype AbstractStorage and implement reset!, push!, times, samples, timespan, and the interpolation callable.\nA new monitor type Subtype AbstractMonitor and implement push!(mon, t, x, force).\n\nThe test suite mirrors the source layout, so new functionality should be paired with new tests in test/test_<area>.jl.","title":"Where to start when contributing"},{"category":"section","location":"internals/#Cross-references","page":"Internals","text":"Architecture — the four-axis decomposition that this page implements.\nAPI — the public docstrings.","title":"Cross-references"},{"category":"section","location":"quadrature/#Quadrature-equations","page":"Quadrature equations","text":"A common task in dynamical systems is to compute integrals of observables along trajectories:\n\nI(T) = int_t_0^T gbigl(mathbfx(t)bigr)mathrmdt\n\nFlows.jl offers two ways to compute such integrals. The first — coupling the quadrature into the system — gives an integral whose accuracy matches the time integrator's order. The second — post-processing samples with a stand-alone rule — gives an integral whose accuracy is determined by the chosen quadrature rule alone. The first is almost always what you want; the second is convenient when the observable is decided after the integration is done.","title":"Quadrature equations"},{"category":"section","location":"quadrature/#Coupling-the-quadrature","page":"Quadrature equations","text":"The recipe is straightforward. Add a quadrature component I(t) to the system whose dynamics is exactly the integrand, with initial condition zero:\n\nleft\nbeginaligned\ndotmathbfx(t) = mathbff(tmathbfx(t))quad mathbfx(t_0) = mathbfx_0\ndotI(t)          = gbigl(mathbfx(t)bigr)hspace105em I(t_0) = 0\nendaligned\nright\n\nIntegrate the augmented system as a Coupled state from t_0 to T. The final value I(T) is the requested integral, evaluated with an error that inherits the order of accuracy of the integrator. A fourth-order primal integrator gives a fourth-order accurate integral; there is no separate quadrature error to worry about.\n\nA minimal example for a three-dimensional state:\n\ng(t, x, dxdt, I, dIdt) = (dIdt[1] = x[1]^2; return dIdt)\n\nx = Float64[1.0, 3.0, 4.0]\nI = Float64[0.0]\n\nF = flow(couple(f, g),\n         RK4(couple(x, I)),\n         TimeStepConstant(0.1))\n\nF(couple(x, I), (0, 10))\nI[1]    # ≈ ∫₀¹⁰ x₁(t)² dt\n\nThe quadrature component is a one-element Vector, because the package requires the state to be a mutable container. To compute several integrals simultaneously, widen the quadrature component to a multi-element vector and fill more slots in dIdt:\n\nfunction quads(t, x, dxdt, I, dIdt)\n    dIdt[1] = x[1]\n    dIdt[2] = x[1]^2\n    dIdt[3] = norm(x)\n    return dIdt\nend\n\nI = zeros(3)\n\nThe default call dependency wires the second component to see the first, which is exactly the right pattern: g's signature is g(t, x, dxdt, I, dIdt) and uses x to fill dIdt. No CallDependency argument is needed.","title":"Coupling the quadrature"},{"category":"section","location":"quadrature/#IMEX-systems-with-an-explicit-quadrature","page":"Quadrature equations","text":"When the primal is stiff and uses an IMEX scheme, the quadrature component is almost always advanced fully explicitly: dotI does not see the linear operator. The idiom is to couple the primal linear operator with nothing for the quadrature component:\n\nL = Diagonal(...)            # the stiff primal linear operator\n\nF = flow(couple(f_ex, g),\n         couple(L, nothing),     # `nothing` means \"no implicit part\"\n         CNRK2(couple(x, I)),\n         TimeStepConstant(0.1))\n\nThe IMEX scheme detects the nothing and collapses the implicit subproblem for the quadrature component to the identity — no mul! call, no ImcA! call. The explicit half does the entire work for the quadrature row. The dispatch is resolved at compile time by System's @generated machinery, so the nothing branch has zero runtime cost.","title":"IMEX systems with an explicit quadrature"},{"category":"section","location":"quadrature/#Why-couple,-not-post-process?","page":"Quadrature equations","text":"Coupling has three concrete advantages over a post-processing rule:\n\nAccuracy. The integrator is, by construction, order-p. Coupling yields an order-p integral. A post-hoc rule has its own (often lower) order.\nSame time grid. The samples that feed the integrand are exactly the values the primal scheme computes anyway. There is no separate sampling decision.\nMemory. A coupled integration carries one extra small component. A post-hoc rule requires storing every sampled value of the observable in a Monitor until the end.\n\nThe cookbook example Lyapunov exponent of the Lorenz attractor uses the coupling idiom to integrate a logarithmic growth rate — see the Cookbook.","title":"Why couple, not post-process?"},{"category":"section","location":"quadrature/#Monitoring-the-quadrature","page":"Quadrature equations","text":"It is sometimes useful to monitor the value of the quadrature variable along the integration — e.g. to plot the cumulative integral, or to compute a running average. The pattern is to monitor the coupled state and extract the quadrature component:\n\nmon = Monitor(couple(x, I), (t, xq) -> xq[2])\nF(couple(x, I), (0, T), mon)\n\nThe monitor receives the coupled state and returns whatever derived quantity you want — here, the full quadrature component (which itself may be a vector of integrals).\n\nThe cumulative average of g(mathbfx(t)) is\n\nbar g(tau) = frac1tauint_0^tau gbigl(mathbfx(t)bigr)mathrmdt\n\nobtained from the recorded samples and times by\n\ng_bar = samples(mon) ./ times(mon)\n\nThe first element is 0/0; either skip it or initialise the monitor with skipfirst=true.","title":"Monitoring the quadrature"},{"category":"section","location":"quadrature/#Post-hoc-quadrature-rules","page":"Quadrature equations","text":"When the observable is decided after an integration has already happened, two stand-alone rules are exported: trapz (composite trapezoidal) and simps (composite Simpson's). Both expect two same-length vectors xs (abscissae, sorted) and ys (integrand values), and return the integral as a scalar.\n\nmon = Monitor(x, (t, x) -> g(x))\nF(x, (0, T), mon)\n\nI_trap = trapz(times(mon), samples(mon))\nI_simp = simps(times(mon), samples(mon))","title":"Post-hoc quadrature rules"},{"category":"section","location":"quadrature/#trapz","page":"Quadrature equations","text":"The composite trapezoidal rule over (possibly non-uniform) intervals:\n\nmathrmtrapz(mathbfxmathbfy) = sum_k=1^N-1 fracy_k+1+y_k2(x_k+1-x_k)\n\nExact for piecewise-linear integrands, O(h^2) error for smooth ones. Robust and never produces a negative weight.","title":"trapz"},{"category":"section","location":"quadrature/#simps","page":"Quadrature equations","text":"The composite Simpson rule with a quadratic correction for the last interval when the number of intervals is odd. Exact for cubics on a uniform grid, O(h^4) for smooth integrands.","title":"simps"},{"category":"section","location":"quadrature/#Choosing-between-the-two","page":"Quadrature equations","text":"For most workflows the choice is dictated by what is recorded:\n\nSituation Use\nTrajectory accurately monitored, smooth observable simps\nTrajectory monitored, sample timing is irregular trapz\nTrajectory sparsely sampled and the integral is qualitative only trapz\nTrajectory monitored once, observable known at integration time Couple it instead\n\nIf accuracy matters, prefer the coupled approach over either post-hoc rule.","title":"Choosing between the two"},{"category":"section","location":"quadrature/#Cross-references","page":"Quadrature equations","text":"Coupled systems — call-dependency rules for the coupled idiom.\nTrajectory data — monitors and storages for sampling along a trajectory.\nCookbook — worked examples of the coupling pattern.","title":"Cross-references"},{"category":"section","location":"api/#Full-public-API","page":"API","text":"Docstrings for every exported and documented internal symbol, grouped by topic. For guidance on which symbol to reach for in which situation, start with the manual pages — this reference is intentionally terse.","title":"Full public API"},{"category":"section","location":"api/#Flow-operator","page":"API","text":"","title":"Flow operator"},{"category":"section","location":"api/#System-wrapper","page":"API","text":"","title":"System wrapper"},{"category":"section","location":"api/#States-and-coupled-states","page":"API","text":"","title":"States and coupled states"},{"category":"section","location":"api/#Call-dependencies","page":"API","text":"","title":"Call dependencies"},{"category":"section","location":"api/#Integration-schemes","page":"API","text":"","title":"Integration schemes"},{"category":"section","location":"api/#Integration-modes","page":"API","text":"","title":"Integration modes"},{"category":"section","location":"api/#IMEX-implicit-solves","page":"API","text":"","title":"IMEX implicit solves"},{"category":"section","location":"api/#Time-stepping","page":"API","text":"","title":"Time stepping"},{"category":"section","location":"api/#Monitors","page":"API","text":"","title":"Monitors"},{"category":"section","location":"api/#Storages","page":"API","text":"","title":"Storages"},{"category":"section","location":"api/#Stage-caches","page":"API","text":"","title":"Stage caches"},{"category":"section","location":"api/#Standalone-quadrature-rules","page":"API","text":"","title":"Standalone quadrature rules"},{"category":"function","location":"api/#Flows.flow","page":"API","text":"flow(g, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) -> Flow\n\nConstruct a Flow representing the numerical discretisation of the time-forward operator associated with the vector field g. The integration scheme is m and the time-stepping policy is ts.\n\nThis method is intended for use with explicit integration methods such as RK4. The optional sym argument is a callable sym(x, s) that applies a symmetry transformation to the flow's result; when omitted, no transformation is applied.\n\n\n\n\n\nflow(g, A, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) -> Flow\n\nConstruct a Flow for a system with linear part A and nonlinear part g. An implicit-explicit integration method m such as CNRK2 or one of the CB* schemes should be supplied. As with the explicit constructor, sym(x, s) is optional and applied via SymTransform to the final state.\n\n\n\n\n\nflow(g::Coupled{N}, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) where {N} -> Flow\n\nConstruct a Flow for a composite vector field g with the default call-dependency pattern, i.e. the right-hand sides satisfy\n\ng[1](t, u[1], dudt[1])\ng[2](t, u[1], dudt[1], u[2], dudt[2])\n…\ng[N](t, u[1], dudt[1], …, u[N], dudt[N])\n\nUse an explicit method such as RK4. See CallDependency for the syntax to express non-default dependency patterns. The optional sym(x, s) is wrapped with CoupledTransform and applied component-wise.\n\n\n\n\n\nflow(g::Coupled{N}, spec::CallDependency{N}, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) where {N} -> Flow\n\nLike the previous flow overload but with an explicit CallDependency spec describing how each component's right-hand-side depends on the other components.\n\n\n\n\n\nflow(g::Coupled{N}, A::Coupled{N}, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) where {N} -> Flow\n\nConstruct an implicit-explicit Flow for a coupled system, using the default call-dependency pattern. A[i] may be nothing for components that should be advanced fully explicitly.\n\n\n\n\n\nflow(g::Coupled{N}, A::Coupled{N}, spec::CallDependency{N}, m::AbstractMethod, ts::AbstractTimeStepping, sym=nothing) where {N} -> Flow\n\nMost general coupled constructor: explicit right-hand sides, linear operators, call dependencies, integration method, time stepping, and optional symmetry, all specified separately.\n\n\n\n\n\n","title":"Flows.flow"},{"category":"type","location":"api/#Flows.Flow","page":"API","text":"(I::Flow)(x, span::NTuple{2, Real}[, s])\n\nMap x from time span[1] to time span[2], optionally applying the symmetry parameterised by s at the end.\n\nx is modified in place. Its type must match the one used to construct the integration method, since the method object holds preallocated buffers of that same type. The optional symmetry parameter s may only be passed when the flow was constructed with a non-trivial sym.\n\n\n\n\n\n(I::Flow)(x, span::NTuple{2, Real}[, s], m::AbstractMonitor)\n\nMap x from span[1] to span[2], recording samples into the monitor m along the way. See the Monitors section of Trajectory data in the manual for how to construct monitors. The optional symmetry parameter s is applied to the final state, exactly as in the basic two-argument form.\n\n\n\n\n\n(I::Flow)(x, span::NTuple{2, Real}[, s], c::AbstractStageCache)\n\nMap x from span[1] to span[2], pushing the internal stage values of each time step into the stage cache c. The cache can later be used to replay the same time grid through a linearised system in a discretely consistent manner. See the Stage caches section of Trajectory data in the manual for details.\n\n\n\n\n\n(I::Flow)(x, span::NTuple{2, Real}[, s], store::AbstractStorage)\n\nMap x from span[1] to span[2], pushing snapshots of the state into the storage store along the way. This is the typical way to build the trajectory required by a continuous adjoint/tangent integration: the storage acts as the source for the linear operator at non-grid times. See the Storages section of Trajectory data in the manual.\n\n\n\n\n\n(I::Flow{TimeStepFromCache})(x, c::AbstractStageCache[, s], m::Union{Nothing, <:AbstractMonitor}=nothing)\n\nReplay the stage cache c through the linearised system held by I, optionally recording samples into the monitor m. The time grid is fully determined by c, so no span argument is required. This produces a discretely consistent linearised/adjoint trajectory.\n\n\n\n\n\n(I::Flow{TimeStepFromStorage})(x, store::AbstractStorage, span::NTuple{2, Real}[, s], m::Union{Nothing, <:AbstractMonitor}=nothing)\n\nMap x from span[1] to span[2] using the trajectory in store to evaluate the linear operator at the times required by the continuous linearised/adjoint scheme. This is not a discretely consistent path — see the TimeStepFromCache variant above for the consistent alternative. Optional m records observables.\n\n\n\n\n\n","title":"Flows.Flow"},{"category":"type","location":"api/#Flows.InvalidSpanError","page":"API","text":"InvalidSpanError(span)\n\nException raised by a Flow call when the requested integration span is invalid. For the non-distributed paths this means the span is strictly decreasing (span[1] > span[2]).\n\n\n\n\n\n","title":"Flows.InvalidSpanError"},{"category":"type","location":"api/#Flows.System","page":"API","text":"System{N, DEPS, GT, AT}\n\nInternal representation of a dynamical system as the sum of an explicit nonlinear term g and an optional linear/implicit term A.\n\nN is the number of coupled equations and DEPS is the CallDependency used to dispatch the generated call into the component right-hand-sides. Constructed by flow; end users should not need to construct it directly.\n\n\n\n\n\n","title":"Flows.System"},{"category":"type","location":"api/#Flows.Coupled","page":"API","text":"Coupled{N, ARGS<:NTuple{N, Any}} <: AbstractVector{Any}\n\nCouple N heterogeneous objects together into a single composite state.\n\nThe components are stored internally in a Julia NTuple of size N. A Coupled object is itself immutable, but its individual elements are expected to be mutable so that in-place updates from a time-stepping scheme can propagate to the underlying storage. Elements are accessed with the standard indexing notation (x[i]), and setindex! is deliberately not defined: the contents of an element may be modified, but the element itself cannot be replaced.\n\nExamples\n\nz = couple(Float64[1, 2, 3], Float64[0, 4])\nz[1]       # → [1.0, 2.0, 3.0]\nlength(z)  # → 2\n\n\n\n\n\n","title":"Flows.Coupled"},{"category":"function","location":"api/#Flows.couple","page":"API","text":"couple(args...) -> Coupled\n\nConstruct a Coupled object from the supplied positional arguments. The order of the arguments fixes the indexing order of the resulting composite state.\n\n\n\n\n\n","title":"Flows.couple"},{"category":"function","location":"api/#Flows.couplecopy","page":"API","text":"couplecopy(N::Int, x) -> Coupled\n\nCouple N independent deep copies of x together. Useful when an algorithm needs N working buffers of the same structure as x but without aliasing them through the same underlying memory.\n\n\n\n\n\n","title":"Flows.couplecopy"},{"category":"type","location":"api/#Flows.SymTransform","page":"API","text":"SymTransform(sym) <: Function\n\nWrap a single-state symmetry transformation so that a Flow object carries a uniform sym field type. sym is expected to be a callable implementing sym(x, s) that applies a symmetry parameterised by s to a non-Coupled state x in place.\n\nWhen constructed with nothing, the constructor collapses to nothing so that flows built without a symmetry have an empty sym field.\n\n\n\n\n\n","title":"Flows.SymTransform"},{"category":"type","location":"api/#Flows.CoupledTransform","page":"API","text":"CoupledTransform(sym) <: Function\n\nWrap a symmetry transformation that should be applied component-wise to a Coupled state. sym(x[i], s) is called for every component i ∈ 1:N. The constructor collapses to nothing when given nothing, so coupled flows built without a symmetry have an empty sym field.\n\n\n\n\n\n","title":"Flows.CoupledTransform"},{"category":"type","location":"api/#Flows.CallDependency","page":"API","text":"CallDependency{N, INFO}\n\nEncode the call-dependency pattern for a composite right-hand-side of N coupled equations. INFO is a tuple of N tuples of positive integers, where the i-th tuple lists the components that participate in the call signature of g[i].\n\nFor example, the default pattern for N = 3,\n\nCallDependency((1,), (1, 2), (1, 2, 3))\n\nmeans that g[1] is called as g[1](t, u[1], dudt[1]), g[2] as g[2](t, u[1], dudt[1], u[2], dudt[2]), and so on — each subsequent component depends on all the preceding ones.\n\nThe type parameter is used to dispatch the generated System call.\n\n\n\n\n\n","title":"Flows.CallDependency"},{"category":"type","location":"api/#Flows.RK4","page":"API","text":"RK4(x::X, mode::AbstractMode = NormalMode()) -> RK4\n\nConstruct a classical fourth-order explicit Runge–Kutta integration scheme. x is a template object used to preallocate the internal stage buffers; the resulting scheme can be applied to any state of the same type.\n\nmode selects forward nonlinear (NormalMode) vs. tangent / adjoint linearised modes (ContinuousMode / DiscreteMode). The number of allocated buffers depends on the mode: 5 for normal, 6 for continuous.\n\n\n\n\n\n","title":"Flows.RK4"},{"category":"type","location":"api/#Flows.CNRK2","page":"API","text":"CNRK2(x::X, mode::AbstractMode = NormalMode()) -> CNRK2\n\nConstruct a second-order Crank–Nicolson / Heun (predictor–corrector) implicit-explicit scheme. The implicit part is treated via ImcA! and the explicit part via the right-hand-side; both must be defined for the system. x is a template object used to preallocate the internal stage buffers (five of them).\n\nmode selects forward nonlinear (NormalMode) vs. tangent / adjoint linearised modes; the discrete-mode adjoint runs the kernel backwards through the cached stages.\n\n\n\n\n\n","title":"Flows.CNRK2"},{"category":"type","location":"api/#Flows.CB3R2R2","page":"API","text":"CB3R2R2(x::X, mode::AbstractMode = NormalMode()) -> CB3R2R2\n\nConstruct the three-register, three-stage, second-order implicit-explicit Runge–Kutta scheme of Cavaglieri & Bewley (2015). The number of stage buffers preallocated from the template x depends on mode: 3 for NormalMode, 4 for ContinuousMode, 6 for DiscreteMode.\n\n\n\n\n\n","title":"Flows.CB3R2R2"},{"category":"type","location":"api/#Flows.CB3R2R3c","page":"API","text":"CB3R2R3c(x::X, mode::AbstractMode = NormalMode()) -> CB3R2R3c\n\nConstruct the three-register, four-stage, third-order (\"c\" variant) IMEX Runge–Kutta scheme of Cavaglieri & Bewley (2015). See CB3R2R2 for the meaning of mode and the stage-buffer counts.\n\n\n\n\n\n","title":"Flows.CB3R2R3c"},{"category":"type","location":"api/#Flows.CB3R2R3e","page":"API","text":"CB3R2R3e(x::X, mode::AbstractMode = NormalMode()) -> CB3R2R3e\n\nConstruct the three-register, four-stage, third-order (\"e\" variant) IMEX Runge–Kutta scheme of Cavaglieri & Bewley (2015). See CB3R2R2 for the meaning of mode and the stage-buffer counts.\n\n\n\n\n\n","title":"Flows.CB3R2R3e"},{"category":"type","location":"api/#Flows.CB4R3R4","page":"API","text":"CB4R3R4(x::X, mode::AbstractMode = NormalMode()) -> CB4R3R4\n\nConstruct the four-register, six-stage, fourth-order IMEX Runge–Kutta scheme of Cavaglieri & Bewley (2015). x is a template used to preallocate the internal stage buffers; the count depends on mode: 4 for NormalMode, 5 for ContinuousMode.\n\n\n\n\n\n","title":"Flows.CB4R3R4"},{"category":"type","location":"api/#Flows.AbstractMode","page":"API","text":"AbstractMode\n\nSupertype for integration-mode tags carried by AbstractMethod subtypes. Concrete subtypes are NormalMode, ContinuousMode and DiscreteMode.\n\n\n\n\n\n","title":"Flows.AbstractMode"},{"category":"type","location":"api/#Flows.NormalMode","page":"API","text":"NormalMode()\n\nMode tag for forward nonlinear integration. The integration scheme allocates only the buffers it needs for the primal step and, if a stage cache is supplied, pushes the internal stages so a linearised sibling can later replay the same time grid.\n\n\n\n\n\n","title":"Flows.NormalMode"},{"category":"type","location":"api/#Flows.ContinuousMode","page":"API","text":"ContinuousMode(isadjoint::Bool = false)\n\nMode tag for continuous linearised integration backed by an AbstractStorage. isadjoint = true selects the adjoint variant, which marches backwards in time. The continuous path interpolates the nonlinear trajectory at the times required by the scheme and is not discretely consistent with the original primal integration.\n\n\n\n\n\n","title":"Flows.ContinuousMode"},{"category":"type","location":"api/#Flows.DiscreteMode","page":"API","text":"DiscreteMode(isadjoint::Bool = false)\n\nMode tag for discretely consistent linearised integration backed by a RAMStageCache. isadjoint = true selects the adjoint variant. The discrete path replays the exact stage values recorded by the primal run, so the linearised solution is the exact derivative (or adjoint of the derivative) of the primal at the time-stepping level.\n\n\n\n\n\n","title":"Flows.DiscreteMode"},{"category":"function","location":"api/#Flows.ImcA!","page":"API","text":"ImcA!(A, c::Real, y, z) -> z\n\nSolve the linear problem (I - c·A) z = y for z, where A is a linear operator, c is a scalar, and I is the identity. The result is written in place into z and returned.\n\nThis is one of the two operator-level primitives the IMEX time schemes rely on (the other being ImcA_mul!). Users with a custom A type should add a specialised method to ImcA! that exploits any structure available. A fallback implementation is provided for LinearAlgebra.Diagonal; everything else raises an informative error.\n\nNotes\n\nThe function name should be read \"I-minus-cee-A\".\n\n\n\n\n\n","title":"Flows.ImcA!"},{"category":"function","location":"api/#Flows.ImcA_mul!","page":"API","text":"ImcA_mul!(A, c::Real, y, z) -> z\n\nCompute z = (I - c·A) y for z, where A is a linear operator, c is a scalar, and I is the identity. The result is written in place into z and returned.\n\nThis pairs with ImcA!: the IMEX schemes use ImcA_mul! to form the right-hand-side of the linear system and ImcA! to solve it. The default implementation calls mul!(z, A, y) and then performs the axpby in place; specialise it only when a faster (I - c·A) y evaluation is available.\n\n\n\n\n\n","title":"Flows.ImcA_mul!"},{"category":"type","location":"api/#Flows.TimeStepConstant","page":"API","text":"TimeStepConstant(Δt::Real)\n\nSpecify that integration should proceed with a constant time step Δt. The step is validated at construction (must be strictly positive) and stored as a Float64.\n\n\n\n\n\n","title":"Flows.TimeStepConstant"},{"category":"type","location":"api/#Flows.TimeStepFromStorage","page":"API","text":"TimeStepFromStorage(Δt::Real)\n\nSpecify that integration should proceed with constant time step Δt while interpolating the linear operator's state from an AbstractStorage. This is the continuous (not discretely consistent) counterpart of TimeStepFromCache, and is the appropriate choice when the linearised system is too long to fit into a stage cache. Validates Δt > 0 at construction.\n\n\n\n\n\n","title":"Flows.TimeStepFromStorage"},{"category":"type","location":"api/#Flows.TimeStepFromCache","page":"API","text":"TimeStepFromCache <: AbstractTimeStepping\n\nSingleton policy used to replay the time grid recorded in an AbstractStageCache through a linearised system. There is no parameter: every (t, Δt, stages) triple is taken from the cache in order, so the linearised path is discretely consistent with the nonlinear path that filled the cache.\n\n\n\n\n\n","title":"Flows.TimeStepFromCache"},{"category":"type","location":"api/#Flows.AbstractTimeStepFromHook","page":"API","text":"AbstractTimeStepFromHook <: AbstractTimeStepping\n\nSupertype for time-stepping policies that defer the choice of step size to a user-supplied hook called at runtime. The hook is invoked once per step with the current right-hand side, linear operator and state, and must return a positive scalar Δt.\n\nSee the Time stepping page in the manual for the usage pattern.\n\n\n\n\n\n","title":"Flows.AbstractTimeStepFromHook"},{"category":"type","location":"api/#Flows.Steps","page":"API","text":"Steps{S, R<:AbstractRange{S}} <: AbstractVector{Tuple{S, S}}\n\nLightweight iterator that enumerates (t, Δt) pairs spanning an integration interval. Constructed from a triple (t0, T, Δt) and internally backed by a Julia Range, so it has unit memory cost regardless of the number of steps.\n\nThe convention is that the user always supplies a positive Δt; the constructor flips the sign as needed when t0 > T, so backward integration shares the same code path.\n\nWhen T is not an exact multiple of Δt away from t0, the iterator is lossy: the final pair has a smaller dt that ends precisely at T, ensuring t + dt ≤ T (or t + dt ≥ T when integrating backwards). The isLossy flag records this case so size and the last getindex can adjust.\n\n\n\n\n\n","title":"Flows.Steps"},{"category":"type","location":"api/#Flows.Monitor","page":"API","text":"Monitor{T, X, S, F, L}\n\nConcrete monitor that records the value of a single observable along a trajectory. The observable is computed by a user-supplied callable f with signature f(t, x) and is stored in store, together with the observation time.\n\nThe standard control knobs are:\n\noneevery::Int: keep one sample every oneevery calls.\nsavebetween::Tuple{Float64, Float64}: only store samples whose time falls in the closed interval savebetween.\nskipfirst::Bool: drop the very first observation. Useful when the initial state is artificial.\nlog::AbstractLogger: a Logger used to print the running state of the monitor to a stream as samples come in.\n\nConstruct via the Monitor outer constructor below.\n\n\n\n\n\n","title":"Flows.Monitor"},{"category":"function","location":"api/#Flows.reset!","page":"API","text":"reset!(cache::RAMStageCache) -> cache\n\nDrop all stored steps, leaving the cache empty and ready to be populated again. Returns cache.\n\n\n\n\n\nreset!(rs::RAMStorage, sizehint::Int=0) -> rs\n\nEmpty the time and sample buffers of rs. The optional sizehint preallocates capacity in both buffers so that subsequent push! calls can avoid reallocation. Returns rs.\n\n\n\n\n\nreset!(mon::Monitor, sizehint::Int=0) -> mon\n\nClear the backing storage of mon and reset its sample counter to zero. The optional sizehint is forwarded to the underlying storage so that subsequent pushes can avoid reallocation. Returns mon.\n\n\n\n\n\n","title":"Flows.reset!"},{"category":"function","location":"api/#Flows.times","page":"API","text":"times(rs::RAMStorage) -> Vector\n\nReturn the vector of times stored in rs, in push order.\n\n\n\n\n\ntimes(mon::Monitor) -> AbstractVector\n\nReturn the times at which the monitor's observable was sampled. Each entry corresponds to one accepted push! call. For a RAMStorage backing this is a standard Vector.\n\n\n\n\n\n","title":"Flows.times"},{"category":"function","location":"api/#Flows.samples","page":"API","text":"samples(rs::RAMStorage) -> Vector\n\nReturn the vector of state snapshots stored in rs, in push order.\n\n\n\n\n\nsamples(mon::Monitor) -> AbstractVector\n\nReturn the recorded observable values, indexed compatibly with times. For a RAMStorage backing this is a standard Vector whose element type is whatever the observable function returns.\n\n\n\n\n\n","title":"Flows.samples"},{"category":"type","location":"api/#Flows.StoreNFromLast","page":"API","text":"StoreNFromLast{N, X, F} <: AbstractMonitor{Float64, X}\n\nA specialised monitor that records exactly one observation, taken N time steps before the final step of an integration. The recorded sample is f(z) where f defaults to Base.copy.\n\nThis is useful for collecting the terminal state of a trajectory and its immediate predecessors — for instance, to seed a linearised adjoint integration starting from a state slightly before the nonlinear endpoint.\n\nType parameters\n\nN: the number of steps before the last to record. N = 0 records the final state itself.\nX: the type of the stored observation, inferred from f(z).\nF: the type of the observable callable.\n\n\n\n\n\n","title":"Flows.StoreNFromLast"},{"category":"type","location":"api/#Flows.Logger","page":"API","text":"Logger{N, O<:IO, F}(io, fstring, logevery) <: AbstractLogger\n\nConcrete logger that prints the state of a Monitor to io using a precompiled Printf.Format. N is the number of observed quantities per sample (which controls the width of the printed row). logevery decimates the output: only every logevery-th sample is emitted, but the header is always printed once before the first row.\n\nConstruct via the Logger outer constructors below.\n\n\n\n\n\n","title":"Flows.Logger"},{"category":"type","location":"api/#Flows.AbstractStorage","page":"API","text":"AbstractStorage{T<:Real, X, DEG}\n\nSupertype for objects that store a time series of states along a trajectory. The type parameters are:\n\nT<:Real — the type used to represent times.\nX       — the type used to represent state snapshots.\nDEG     — the degree of the Lagrange polynomial used by the             interpolator built on top of the storage.\n\nConcrete subtypes must implement reset!, Base.push!, times, samples, and timespan. Concrete subtypes that need to interpolate between samples must also be callable with the signature (store)(out, t, ::Val{ORD}).\n\n\n\n\n\n","title":"Flows.AbstractStorage"},{"category":"type","location":"api/#Flows.RAMStorage","page":"API","text":"RAMStorage{T, X, DEG, Vt, Vx} <: AbstractStorage{T, X, DEG}\n\nConcrete in-memory implementation of AbstractStorage. Times and samples are kept in two parallel Vectors, indexed identically.\n\nThe internal flag storelast controls whether the very last sample of an integration is pushed: it is left to the caller to decide because some workflows (notably periodic problems where the period is known) prefer to omit the repeated endpoint. The period field is set to a non-zero value when the data should be treated as one period of a periodic signal; the interpolator then performs modular wrap-around when the requested time is near an endpoint.\n\nConstruct via the RAMStorage outer constructors.\n\n\n\n\n\n","title":"Flows.RAMStorage"},{"category":"function","location":"api/#Flows.period","page":"API","text":"period(rs::RAMStorage) -> Real\n\nReturn the period of the data stored in rs, or zero if the data is not periodic.\n\n\n\n\n\n","title":"Flows.period"},{"category":"function","location":"api/#Flows.isperiodic","page":"API","text":"isperiodic(rs::RAMStorage) -> Bool\n\nReturn true when the storage represents a periodic signal (i.e. period(rs) > 0).\n\n\n\n\n\n","title":"Flows.isperiodic"},{"category":"function","location":"api/#Flows.timespan","page":"API","text":"timespan(rs::RAMStorage) -> Tuple{Real, Real}\n\nReturn the (first, last) time of the stored data. For periodic storages the second element is period(rs) rather than the last sample time, because the storage convention is to omit the repeated endpoint.\n\n\n\n\n\n","title":"Flows.timespan"},{"category":"function","location":"api/#Flows.storelast","page":"API","text":"storelast(rs::RAMStorage) -> Bool\n\nReturn the storelast flag of rs. When false, the propagation loop is expected to omit the final sample of an integration — this is how the no-duplicated-endpoint convention for periodic storage is expressed.\n\n\n\n\n\n","title":"Flows.storelast"},{"category":"function","location":"api/#Flows.degree","page":"API","text":"degree(store::AbstractStorage) -> Int\n\nReturn the polynomial degree DEG used by the storage's Lagrange interpolator.\n\n\n\n\n\n","title":"Flows.degree"},{"category":"type","location":"api/#Flows.AbstractStageCache","page":"API","text":"AbstractStageCache{NS, X}\n\nSupertype for objects that buffer the internal stage values produced by an integration scheme so a linearised counterpart can be replayed on exactly the same time grid.\n\nNS is the number of internal stages produced per time step (and must match the integration scheme); X is the type of one stage value (typically the state type). Concrete subtypes must implement Base.push!(cache, t, Δt, stages::NTuple{NS, X}) and the reset! operation.\n\n\n\n\n\n","title":"Flows.AbstractStageCache"},{"category":"type","location":"api/#Flows.RAMStageCache","page":"API","text":"RAMStageCache{NS, X} <: AbstractStageCache{NS, X}\n\nConcrete in-memory implementation of AbstractStageCache. Three parallel vectors hold the start time, step size, and stage values of each accepted step. Construct with one of the outer constructors below.\n\n\n\n\n\n","title":"Flows.RAMStageCache"},{"category":"function","location":"api/#Flows.trapz","page":"API","text":"trapz(xs, ys) -> Real\n\nApproximate the integral ∫ ys d xs using the composite trapezoidal rule over the (possibly non-uniform) abscissa xs. xs is assumed to be sorted; ys must have the same length and contain the values of the integrand at the corresponding abscissa.\n\nFor uniform xs this reduces to the familiar (h/2)(y₀ + 2y₁ + … + 2yₙ₋₁ + yₙ).\n\n\n\n\n\n","title":"Flows.trapz"},{"category":"function","location":"api/#Flows.simps","page":"API","text":"simps(xs, ys) -> Real\n\nApproximate the integral ∫ ys d xs using the composite Simpson's rule over the (possibly non-uniform) abscissa xs. xs is assumed to be sorted; ys must have the same length and contain the values of the integrand at the corresponding abscissa.\n\nWhen the number of intervals is odd, the last interval is closed with a three-point quadratic correction so the overall rule remains accurate without dropping a sample.\n\n\n\n\n\n","title":"Flows.simps"},{"category":"section","location":"cookbook/#Cookbook","page":"Cookbook","text":"End-to-end worked examples. Each entry is self-contained: it states the problem, derives whatever mathematics is needed, sets up the Flows.jl flow, runs the integration, and reports the result.\n\nThe examples are arranged roughly by increasing complexity. The first three exercise the forward interface; the middle three exercise the coupled and linearised paths; the last two exercise symmetry reduction and stiff integration.\n\n# Example Concepts\n1 Lorenz attractor — forward integration Flow construction, monitor, times / samples\n2 Lorenz with a running average via quadrature Coupled systems, quadrature equation\n3 Custom adaptive time-step controller AbstractTimeStepFromHook\n4 Sensitivity to initial conditions of the Lorenz system Coupled forward tangent path\n5 Lyapunov exponent of the Lorenz attractor Tangent + quadrature + re-normalisation via monitor\n6 Discrete adjoint sensitivity for an optimal-control problem Stage cache, DiscreteMode(true), TimeStepFromCache\n7 Continuous adjoint for a stiff diffusion problem RAMStorage, ContinuousMode(true), TimeStepFromStorage, IMEX\n8 Stiff diffusion with CNRK2 IMEX setup, Diagonal linear operator\n\nThe complete code for each example can be copy-pasted into a Julia REPL with Flows.jl installed.\n\n","title":"Cookbook"},{"category":"section","location":"cookbook/#Lorenz-attractor-—-forward-integration","page":"Cookbook","text":"The canonical \"Hello, world!\" for a flow library. We define the Lorenz system and integrate it from a random initial condition, recording the trajectory.\n\nusing Flows, LinearAlgebra\n\nstruct Lorenz\n    σ::Float64; ρ::Float64; β::Float64\nend\n\nfunction (eq::Lorenz)(t, x, dxdt)\n    dxdt[1] = eq.σ * (x[2] - x[1])\n    dxdt[2] = eq.ρ * x[1] - x[2] - x[1]*x[3]\n    dxdt[3] = -eq.β * x[3] + x[1]*x[2]\n    return dxdt\nend\n\nF   = flow(Lorenz(10.0, 28.0, 8/3),\n           RK4(zeros(3)),\n           TimeStepConstant(1e-2))\n\nmon = Monitor(zeros(3), (t, x) -> copy(x))\nx   = rand(3)\nF(x, (0.0, 50.0), mon)\n\nts  = times(mon)\nxs  = samples(mon)\n\nxs is a Vector{Vector{Float64}} ready to be plotted; ts is the matching time vector.\n\n","title":"Lorenz attractor — forward integration"},{"category":"section","location":"cookbook/#Lorenz-with-a-running-average-via-quadrature","page":"Cookbook","text":"We want the time-average langle x_3rangle = (1T)int_0^T x_3(t)mathrmdt along a Lorenz trajectory. The coupled-quadrature recipe yields a fourth-order accurate result with no quadrature-rule overhead.\n\nquad(t, x, dxdt, I, dIdt) = (dIdt[1] = x[3]; return dIdt)\n\nF = flow(couple(Lorenz(10.0, 28.0, 8/3), quad),\n         RK4(couple(zeros(3), zeros(1))),\n         TimeStepConstant(1e-2))\n\nx = rand(3)\nI = [0.0]\nF(couple(x, I), (0.0, 100.0))\n\navg = I[1] / 100.0    # time-averaged ⟨x₃⟩\n\nTo monitor the running average bar g(tau) = I(tau)tau along the trajectory, attach a monitor that extracts the quadrature component:\n\nmon = Monitor(couple(x, I), (t, xq) -> xq[2][1] / max(t, eps()))\nF(couple(x, I), (0.0, 100.0), mon)\n\n# samples(mon)[end]  →  the same `avg` as above\n\n","title":"Lorenz with a running average via quadrature"},{"category":"section","location":"cookbook/#Custom-adaptive-time-step-controller","page":"Cookbook","text":"A constant time-step works for Lorenz at moderate rho, but for some PDE problems a CFL-like constraint changes with the state. The example below caps the step at a target CFL number computed from the state norm.\n\nstruct CFLHook <: AbstractTimeStepFromHook\n    cmax::Float64\nend\n\n(h::CFLHook)(g, A, z) = h.cmax / (maximum(abs, z) + eps())\n\nF = flow(Lorenz(10.0, 28.0, 8/3),\n         RK4(zeros(3)),\n         CFLHook(0.5))\n\nx = rand(3)\nF(x, (0.0, 50.0))\n\nThe hook returns whatever Δt is appropriate for the next step; the integrator clamps it so the requested endpoint is hit exactly.\n\n","title":"Custom adaptive time-step controller"},{"category":"section","location":"cookbook/#Sensitivity-to-initial-conditions-of-the-Lorenz-system","page":"Cookbook","text":"The flow Jacobian J^tau(mathbfx_0) maps an initial-condition perturbation mathbfy_0 to its propagated form mathbfy(t_0+tau). The coupled-forward path computes J^taumathbfy_0 in a single pass.\n\n# tangent right-hand-side: f_y' = J_x f · y\nfunction lorenz_tan(t, x, dxdt, y, dydt)\n    σ, ρ, β = 10.0, 28.0, 8/3\n    dydt[1] = σ * (y[2] - y[1])\n    dydt[2] = (ρ - x[3]) * y[1] - y[2] - x[1] * y[3]\n    dydt[3] = x[2] * y[1] + x[1] * y[2] - β * y[3]\n    return dydt\nend\n\nF = flow(couple(Lorenz(10.0, 28.0, 8/3), lorenz_tan),\n         RK4(couple(zeros(3), zeros(3))),\n         TimeStepConstant(1e-3))\n\nx = rand(3)\ny = [1.0, 0.0, 0.0]                    # perturb x[1] direction\nF(couple(x, y), (0.0, 1.0))\n\ny    # ≈ J^{1.0}(x₀) · [1, 0, 0]\n\nTo get the full 3times 3 Jacobian, run the integration three times with three basis vectors for mathbfy_0 and stack the results.\n\n","title":"Sensitivity to initial conditions of the Lorenz system"},{"category":"section","location":"cookbook/#Lyapunov-exponent-of-the-Lorenz-attractor","page":"Cookbook","text":"The maximum Lyapunov exponent lambda_1 measures the average exponential growth rate of an infinitesimal perturbation along the attractor:\n\nlambda_1 = lim_Ttoinftyfrac1Tint_0^T fraclanglemathbfymathcalJmathbfyranglelanglemathbfymathbfyranglemathrmdt\n\nwith mathbfy(t) the tangent perturbation re-normalised to unit length frequently enough that it does not overflow.\n\nFlows.jl makes this three-line:\n\na primal (Lorenz),\na tangent (linearised Lorenz),\na quadrature recording the instantaneous growth rate,\n\nre-normalising the tangent periodically inside a monitor callback.\n\nfunction growth_rate(t, x, dxdt, y, dydt, λ, dλdt)\n    # advance the primal\n    lorenz_rhs(t, x, dxdt)                            # fill dxdt\n    # advance the tangent\n    lorenz_tan(t, x, dxdt, y, dydt)                    # fill dydt\n    # instantaneous logarithmic growth: ⟨y, J·y⟩ / ⟨y, y⟩\n    dλdt[1] = dot(y, dydt) / dot(y, y)\n    return dλdt\nend\n\ndeps = CallDependency((1,), (1, 2), (1, 2, 3))\n\nF = flow(couple(lorenz_rhs, lorenz_tan, growth_rate), deps,\n         RK4(couple(zeros(3), zeros(3), zeros(1))),\n         TimeStepConstant(1e-3))\n\n# normalise tangent every 100 steps to avoid overflow\nmon = Monitor(couple(zeros(3), zeros(3), zeros(1)),\n              (t, z) -> (normalize!(z[2]); 0.0);\n              oneevery=100)\n\nx, y, λ = rand(3), normalize!(rand(3)), [0.0]\nF(couple(x, y, λ), (0.0, 500.0), mon)\n\nλ_1 = λ[1] / 500.0       # ≈ 0.906 for ρ=28\n\nThis uses every coupled-system feature: the call-dependency override, the per-component RHS, and the monitor-as-callback re-normalisation.\n\n","title":"Lyapunov exponent of the Lorenz attractor"},{"category":"section","location":"cookbook/#Discrete-adjoint-sensitivity-for-an-optimal-control-problem","page":"Cookbook","text":"A textbook adjoint sensitivity computation, derived in detail in the adjoint sensitivity section of Mathematical foundations. The objective is\n\nmathcalJ(mathbfx_0) = int_0^T sinbigl(x(t)bigr)mathrmdt\n\nfor the toy scalar problem dot x = x^2 + u(t), with the goal of computing mathrmdmathcalJmathrmdu(t).\n\nThe discrete adjoint path uses a stage cache:\n\n# 1. primal: cache the stages\ncache = RAMStageCache(nstages(RK4), zeros(1))\nF     = flow(toy_rhs, RK4(zeros(1)), TimeStepConstant(0.01))\nx     = [1.0]\nF(x, (0.0, 1.0), cache)\n\n# 2. adjoint: terminal condition q(T) = 0, march backwards\nA = flow(toy_adj,\n         RK4(zeros(1), DiscreteMode(true)),\n         TimeStepFromCache())\nq = [0.0]\nA(q, cache)              # q now contains the discrete adjoint over the grid\n\n# 3. gradient: dJ/du(t) = -q(t)\ngrad = -q[1]\n\nThe discrete-adjoint identity guarantees that grad is the exact derivative of the discretised objective with respect to the discretised control. This is the property a Newton optimiser will reward you for.\n\n","title":"Discrete adjoint sensitivity for an optimal-control problem"},{"category":"section","location":"cookbook/#Continuous-adjoint-for-a-stiff-diffusion-problem","page":"Cookbook","text":"The setup is a one-dimensional heat equation partial_t u = nupartial_x^2 u + s(t u) discretised in Fourier space, where the linear part is stiff and the source s is mild. We want the gradient of mathcalJ = int_0^T u(t)^2mathrmdt with respect to a control parameter that enters s.\n\nThe CB3R2R3e scheme handles the IMEX primal cheaply; the storage records step-end snapshots, and a ContinuousMode(true) flow integrates the adjoint backwards against the interpolated primal.\n\n# primal in Fourier space\nν, N = 0.01, 64\nL    = Diagonal(-ν .* (0:N-1).^2)         # stiff diffusive operator\ns    = (t, û, dûdt) -> ...                # the explicit part\nF    = flow(s, L, CB3R2R3e(zeros(ComplexF64, N)), TimeStepConstant(1e-2))\n\nstore = RAMStorage(zeros(ComplexF64, N); degree=5)\nû     = randn(ComplexF64, N)\nF(û, (0.0, 1.0), store)\n\n# continuous adjoint: ContinuousMode(true) with TimeStepFromStorage\nadj_rhs = (t, û_interp, q, dqdt) -> ...\n\nA = flow(adj_rhs, L', CB3R2R3e(zeros(ComplexF64, N), ContinuousMode(true)),\n         TimeStepFromStorage(1e-2))\n\nq = zeros(ComplexF64, N)              # terminal condition\nA(q, store, (1.0, 0.0))               # backward span signals reverse time\n\nThe adjoint flow uses its own time step (here, the same as the primal but it could differ) and interpolates the primal at every internal stage time of every step.\n\n","title":"Continuous adjoint for a stiff diffusion problem"},{"category":"section","location":"cookbook/#Stiff-diffusion-with-CNRK2","page":"Cookbook","text":"A self-contained example of using an IMEX scheme on a finite-difference discretisation of the heat equation. The stiff linear part is a tridiagonal Laplacian; the rest is a small nonlinear source.\n\nFor brevity we use Diagonal to stand in for the tridiagonal, since Flows.jl ships an ImcA! for it out of the box:\n\nusing LinearAlgebra\n\nN   = 64\nν   = 1.0\nk   = (0:N-1)\nL   = Diagonal(-ν .* k.^2)                       # spectral diffusion\nsrc = (t, û, dûdt) -> (dûdt .= 0.01 .* sin.(û); dûdt)\n\nF   = flow(src, L, CNRK2(zeros(ComplexF64, N)), TimeStepConstant(1e-2))\n\nû   = randn(ComplexF64, N)\nF(û, (0.0, 1.0))\n\nDiagonal is the only out-of-the-box linear operator. For non-diagonal stiff parts (a real tridiagonal Laplacian, a banded matrix, …) the user provides a struct, a LinearAlgebra.mul! method, and an ImcA! method that solves (I - cmathcalL)mathbfz = mathbfy. See States and vector fields.\n\n","title":"Stiff diffusion with CNRK2"},{"category":"section","location":"cookbook/#Further-reading","page":"Cookbook","text":"Mathematical foundations for the equations behind the examples.\nLinearised dynamics for the full discussion of the discrete-vs-continuous adjoint choice.\nArchitecture and Internals for the implementation details exposed by these examples.","title":"Further reading"},{"category":"section","location":"quickstart/#Quick-start","page":"Quick start","text":"This page shows the shortest path from \"I have a vector field\" to \"I am running an integration\". The example uses the classical Lorenz system, but every step transfers directly to systems of arbitrary size and structure.\n\nIf you need a deeper explanation of the design before you start, jump to Mathematical foundations and Architecture. Otherwise, follow the four steps below.","title":"Quick start"},{"category":"section","location":"quickstart/#1.-Define-the-state","page":"Quick start","text":"A state in Flows.jl is any Julia object that satisfies two contracts: it can be copy-ed and similar-ed, and it supports in-place dot-broadcasting (x .= a .* y .+ b .* z). Plain Julia arrays satisfy both out of the box, so for our Lorenz state we use a length-3 Vector{Float64}:\n\nx = zeros(3)\n\nUser-defined types (a SpectralField, a Grid, …) also work as long as they satisfy these two contracts. See States and vector fields.","title":"1. Define the state"},{"category":"section","location":"quickstart/#2.-Define-the-vector-field","page":"Quick start","text":"The vector field is any callable with signature\n\nf(t::Real, x, dxdt) -> dxdt\n\nIt must overwrite dxdt with dx/dt evaluated at (t, x). It must not mutate x. For Lorenz:\n\nstruct Lorenz\n    ρ::Float64\nend\n\nfunction (eq::Lorenz)(t, x, dxdt)\n    σ, β = 10.0, 8/3\n    dxdt[1] = σ * (x[2] - x[1])\n    dxdt[2] = eq.ρ * x[1] - x[2] - x[1]*x[3]\n    dxdt[3] = -β * x[3] + x[1]*x[2]\n    return dxdt\nend\n\nThere is no requirement to use a struct: a plain function or a closure works equally well. The struct is useful when the right-hand side carries parameters or owns auxiliary buffers.","title":"2. Define the vector field"},{"category":"section","location":"quickstart/#3.-Build-the-flow-operator","page":"Quick start","text":"Bundle a vector field, an integration scheme, and a time-stepping policy into a Flows.Flow:\n\nusing Flows\n\nF = flow(Lorenz(28.0), RK4(zeros(3)), TimeStepConstant(1e-2))\n\nThe pieces:\n\nLorenz(28.0) is the right-hand side.\nRK4(zeros(3)) is the integration scheme. Passing zeros(3) tells the scheme to preallocate its internal stage buffers as Vector{Float64} of length 3, matching the state.\nTimeStepConstant(1e-2) is the time-stepping policy: march with a fixed Δt = 0.01.\n\nF is callable.","title":"3. Build the flow operator"},{"category":"section","location":"quickstart/#4.-Run-the-integration","page":"Quick start","text":"x = rand(3)\nF(x, (0.0, 50.0))    # advances x in place from t=0 to t=50\n\nx is overwritten with the state at T = 50. Nothing is allocated on the hot path — the scheme reuses its internal buffers, and the time loop walks an iterator-of-(t, Δt) pairs that has no heap footprint of its own.","title":"4. Run the integration"},{"category":"section","location":"quickstart/#Compose-with-a-monitor","page":"Quick start","text":"To record samples along the trajectory, attach a Monitor:\n\nusing LinearAlgebra\n\nx   = rand(3)\nmon = Monitor(x, (t, x) -> norm(x))   # observable = state norm\nF(x, (0.0, 50.0), mon)\n\nsamples(mon)   # Vector{Float64} of norms, one per accepted step\ntimes(mon)     # corresponding times\n\nThe observable is any callable f(t, x). Its return value is what gets stored; the storage element type is inferred at monitor construction time.","title":"Compose with a monitor"},{"category":"section","location":"quickstart/#Compose-with-a-storage","page":"Quick start","text":"If you need to replay the trajectory later (e.g. to drive a linearised solve), use a RAMStorage instead of (or in addition to) a monitor:\n\nx     = rand(3)\nstore = RAMStorage(x)\nF(x, (0.0, 50.0), store)\n\ntimes(store)    # sampled times\nsamples(store)  # state snapshots\n\nA populated storage is callable: store(out, t) interpolates the trajectory at any t in timespan(store), writing into the preallocated buffer out. This is the entry point for the continuous tangent / adjoint integrators of Linearised dynamics.","title":"Compose with a storage"},{"category":"section","location":"quickstart/#Next-steps","page":"Quick start","text":"You now have the full picture of the \"forward\" interface. The rest of the manual unpacks each piece, and adds:\n\nIMEX schemes for stiff problems — see Integration schemes.\nCoupling several state components — see Coupled systems.\nRecording stage values for discretely consistent linearisation — see Trajectory data and Linearised dynamics.\nComputing trajectory integrals — see Quadrature equations.\nWorked end-to-end examples — see the Cookbook.","title":"Next steps"},{"category":"section","location":"schemes/#Integration-schemes","page":"Integration schemes","text":"Flows.jl ships six concrete schemes: one classical explicit method and five implicit-explicit (IMEX) methods. They all share the same constructor shape and the same step! contract, so a flow is built by picking the scheme that matches the stiffness and the accuracy of the problem at hand. The schemes themselves are dumb data carriers — they hold preallocated stage buffers and implement step! — and they compose freely with any of the Time stepping policies.\n\nThis page catalogues the schemes, gives the trade-offs that should drive the choice between them, and documents the memory footprint of each.","title":"Integration schemes"},{"category":"section","location":"schemes/#How-to-pick","page":"Integration schemes","text":"Question Answer\nIs there a stiff linear part? If yes, an IMEX scheme; if no, RK4.\nDo you need the simplest possible IMEX scheme to validate against? CNRK2 — predictor–corrector second order.\nDo you need an IMEX scheme with minimal memory footprint for large states? One of the CB low-storage family below.\nDo you need fourth-order accuracy? RK4 (explicit) or CB4R3R4 (IMEX).\nDo you need to drive a discretely-consistent tangent/adjoint solve? Any of them — every scheme has a DiscreteMode step!.\n\nIf you are not sure, start with RK4 (no stiff part) or CB3R2R3e (with a stiff part). Both are robust default choices.","title":"How to pick"},{"category":"section","location":"schemes/#Common-interface","page":"Integration schemes","text":"Every scheme has the same constructor shape:\n\nSCHEME(x::X, mode::AbstractMode = NormalMode()) -> SCHEME{X, MODE, NX}\n\nArgument Role\nx Template object used to preallocate all internal stage buffers as similar(x). Locks the state type for the lifetime of the scheme.\nmode An Flows.AbstractMode tag selecting forward / tangent / adjoint behaviour. Default is NormalMode.\n\nThe mode controls only how many buffers are allocated and which step! method is dispatched. Constructing the same scheme with different modes is the standard way to obtain a primal flow and its tangent/adjoint companions:\n\nprimal  = flow(f, RK4(zeros(3)),                            TimeStepConstant(0.01))\ntangent = flow(f, RK4(zeros(3), DiscreteMode(false)),       TimeStepFromCache())\nadjoint = flow(f, RK4(zeros(3), DiscreteMode(true)),        TimeStepFromCache())\n\nEvery scheme provides NS (stage count) via nstages(method). This is the number a stage cache must be configured with, and is matched at integration time.","title":"Common interface"},{"category":"section","location":"schemes/#RK4-—-classical-explicit","page":"Integration schemes","text":"Classical four-stage, fourth-order Runge–Kutta. The canonical default for non-stiff problems.\n\nbeginaligned\nmathbfk_1 = mathbff(tmathbfx_n)\nmathbfk_2 = mathbff(t + tfracDelta t2mathbfx_n + tfracDelta t2mathbfk_1)\nmathbfk_3 = mathbff(t + tfracDelta t2mathbfx_n + tfracDelta t2mathbfk_2)\nmathbfk_4 = mathbff(t + Delta tmathbfx_n + Delta tmathbfk_3)\nmathbfx_n+1 = mathbfx_n + tfracDelta t6bigl(mathbfk_1 + 2mathbfk_2 + 2mathbfk_3 + mathbfk_4bigr)\nendaligned\n\n \nType Explicit Runge–Kutta\nOrder 4\nStages 4\nStage buffers 5 (NormalMode), 6 (ContinuousMode)\nStage cache NS 4\n\nUse RK4 whenever the linear part is absent or its contribution to the stability constraint is comparable to the nonlinear part. For convection-dominated problems, the explicit-CFL constraint is essentially the same as for any other classical RK scheme.","title":"RK4 — classical explicit"},{"category":"section","location":"schemes/#CNRK2-—-Crank–Nicolson-/-Heun-predictor–corrector","page":"Integration schemes","text":"A two-stage second-order IMEX scheme: predictor with explicit Euler on mathbff_mathrmex and trapezoidal on mathcalL, corrector with Heun on mathbff_mathrmex and trapezoidal on mathcalL. Algebraically:\n\nbeginaligned\ntildemathbfx = bigl(I - tfracDelta t2mathcalLbigr)^-1bigl(I + tfracDelta t2mathcalL)mathbfx_n + Delta tmathbff_mathrmex(tmathbfx_n)bigr\nmathbfx_n+1 = bigl(I - tfracDelta t2mathcalLbigr)^-1bigl(I + tfracDelta t2mathcalL)mathbfx_n + tfracDelta t2mathbff_mathrmex(tmathbfx_n) + tfracDelta t2mathbff_mathrmex(t+Delta ttildemathbfx)bigr\nendaligned\n\n \nType IMEX, predictor–corrector\nOrder 2\nImplicit stages 2 (one per ImcA! solve)\nStage buffers 5\nStage cache NS 2\n\nCNRK2 is the right choice when you want the simplest IMEX scheme to validate a stiff problem before moving to one of the higher-order CB variants, or when third-order accuracy is not needed and you want the smallest stage count. It is also the cheapest IMEX option per step in terms of mathbff_mathrmex evaluations.","title":"CNRK2 — Crank–Nicolson / Heun predictor–corrector"},{"category":"section","location":"schemes/#Cavaglieri–Bewley-low-storage-IMEX-schemes","page":"Integration schemes","text":"Four schemes from Cavaglieri & Bewley (2015) [CB]: a family of low-storage IMEX Runge–Kutta methods explicitly designed for spatially-discretised PDE solvers where the state is very large and memory bandwidth dominates the cost. The \"low-storage\" qualifier refers to the very small number of full-state buffers each scheme owns at runtime — three or four — independent of the stage count. The trade-off is that the schemes are derived to admit a specific buffer-recycling pattern; their tableaux are not arbitrary.\n\nThe constants CB2, CB3e, CB3c, CB4 in src/tableaux.jl hold the tableaux in exact rational form; they are converted once to Float64 at module load.\n\n[CB]: Cavaglieri, D. and Bewley, T., 2015. Low-storage implicit/explicit Runge–Kutta schemes for the simulation of stiff high-dimensional ODE systems. Journal of Computational Physics, 286, pp. 172–193.\n\nThe four schemes are summarised in the table below.\n\nScheme Order Stages Register pattern Buffers (normal) Buffers (continuous) Buffers (discrete adjoint)\nCB3R2R2 2 3 3R, 2R 3 4 6\nCB3R2R3e 3 4 3R, 2R 3 4 6\nCB3R2R3c 3 4 3R, 2R 3 4 6\nCB4R3R4 4 6 4R, 3R 4 5 (no discrete kernel yet)\n\n\"3R, 2R\" means three registers for the implicit part and two for the explicit part — the explanation of the register accounting is in the Cavaglieri–Bewley paper. From the user's point of view, the takeaway is that the per-step memory cost of CB3R2R* is three state-sized buffers plus the auxiliary vectors needed for the chosen mode, regardless of stage count.","title":"Cavaglieri–Bewley low-storage IMEX schemes"},{"category":"section","location":"schemes/#CB3R2R2-—-second-order,-three-stage","page":"Integration schemes","text":"The cheapest of the family and the IMEX equivalent of CNRK2 in terms of accuracy. Pick CB3R2R2 over CNRK2 when you want the low-storage register pattern and a slightly larger stability domain, at the price of one extra stage and an extra ImcA! solve per step.","title":"CB3R2R2 — second-order, three-stage"},{"category":"section","location":"schemes/#CB3R2R3e-—-third-order,-four-stage-(\"e\"-variant)","page":"Integration schemes","text":"The standard recommendation as a robust third-order IMEX scheme for spatially-discretised PDEs. The \"e\" subscript denotes one of two third-order tableaux in the original paper.","title":"CB3R2R3e — third-order, four-stage (\"e\" variant)"},{"category":"section","location":"schemes/#CB3R2R3c-—-third-order,-four-stage-(\"c\"-variant)","page":"Integration schemes","text":"The companion to CB3R2R3e, with a slightly different coefficient distribution that may behave better on some stiff problems. The two are interchangeable for most purposes; if CB3R2R3e shows accuracy degradation on your problem at moderate Delta t, try CB3R2R3c.","title":"CB3R2R3c — third-order, four-stage (\"c\" variant)"},{"category":"section","location":"schemes/#CB4R3R4-—-fourth-order,-six-stage","page":"Integration schemes","text":"The most accurate IMEX scheme in the package. Six stages, four state-sized buffers in NormalMode. Worth the extra cost when fourth-order accuracy is required on a stiff problem or when long-time integrations need a low temporal error budget.","title":"CB4R3R4 — fourth-order, six-stage"},{"category":"section","location":"schemes/#Order-verification","page":"Integration schemes","text":"The package's test suite exercises each scheme against a linear scalar problem with known exact solution, verifying that the leading error term scales as Delta t^p where p is the nominal order. See test/test_steps.jl and test/test_integrator.jl.","title":"Order verification"},{"category":"section","location":"schemes/#Stage-caches","page":"Integration schemes","text":"Every scheme integrates with the NormalMode step! overload by default. Supplying a RAMStageCache at integration time activates the caching branch: the scheme records the internal-stage values at every accepted step.\n\ncache = RAMStageCache(nstages(RK4), zeros(3))\nF(x, (0, 1), cache)\n\nThe cached stages are exactly the inputs a DiscreteMode replay of the same scheme requires; this is the mechanism by which Flows.jl produces discretely consistent tangent and adjoint integrations. See Trajectory data and Linearised dynamics.","title":"Stage caches"},{"category":"section","location":"schemes/#Memory-usage","page":"Integration schemes","text":"The dominant memory cost of any scheme is the store::NTuple{N, X} of stage buffers. The cookbook example Stiff diffusion with CNRK2 in the Cookbook walks through the full state-buffer accounting for a 1D PDE example.\n\nIf memory is a constraint, the CB family is the right starting point: their stage counts are dictated by the order, but their buffer counts are independent of the order. CB3R2R3e and CB4R3R4 give you third- and fourth-order accuracy at the same buffer cost as a much cruder scheme.","title":"Memory usage"},{"category":"section","location":"schemes/#Cross-references","page":"Integration schemes","text":"Mathematical foundations — Butcher form, IMEX splitting.\nTime stepping — picks the policy that drives the chosen scheme.\nLinearised dynamics — full discussion of tangent/adjoint modes.\nInternals — buffer accounting and @generated dispatch details.","title":"Cross-references"},{"category":"section","location":"states/#States-and-vector-fields","page":"States and vector fields","text":"This page describes the two things the user is responsible for providing: the state type that represents one snapshot of the system, and the callable that evaluates the right-hand-side of the ODE. Everything else in the package — the schemes, the storages, the time-stepping policies, the linearised integrators — composes around them.\n\nThe page covers:\n\nThe contract a state type must satisfy.\nThe signatures the integrators expect from the vector field.\nThe additional requirements when an IMEX scheme splits off a linear part.\nThe signatures for coupled systems.","title":"States and vector fields"},{"category":"section","location":"states/#State-requirements","page":"States and vector fields","text":"Flows.jl is deliberately polymorphic over the state type. There is no AbstractState supertype; a state is anything that satisfies the following contract.\n\nMethod Why it is needed\nBase.similar(x) The schemes preallocate their stage buffers as similar(x) at construction time.\nBase.copy(x) The _propagate! routines push copies into monitors and storages.\nDot-broadcasting Every scheme's update step is written as y .= a .* x .+ b .* k.\n\nThat is the whole contract. There is no requirement to subtype AbstractArray, no requirement to implement length, size, or scalar indexing — and no requirement to be a Vector.","title":"State requirements"},{"category":"section","location":"states/#Built-in:-plain-Julia-arrays","page":"States and vector fields","text":"Standard AbstractArrays of any rank satisfy the contract out of the box. So do Vectors of complex numbers, BitMatrixes, and (with caveats around mutability) anything else in Base.\n\nF = flow(f, RK4(zeros(64, 64)), TimeStepConstant(1e-3))\nF(rand(64, 64), (0, 1))\n\nIf your state lives in a static-size container (StaticArrays.SArray), it is not a valid state type because it is immutable: dot-broadcasting allocates a new object rather than overwriting in place. Either use the mutable StaticArrays.MArray variant or wrap your data in a plain Vector.","title":"Built-in: plain Julia arrays"},{"category":"section","location":"states/#Custom:-user-defined-state-types","page":"States and vector fields","text":"For PDE or spectral solvers it is common to wrap the underlying storage in a domain-aware type. The recipe is to forward similar, copy, and broadcasting to the inner storage. A minimal example:\n\nstruct SpectralField{T<:Complex} <: AbstractVector{T}\n    data::Vector{T}\nend\n\n# Bare-minimum AbstractArray plumbing\nBase.size(u::SpectralField)              = size(u.data)\nBase.getindex(u::SpectralField, i::Int)  = u.data[i]\nBase.setindex!(u::SpectralField, v, i::Int) = (u.data[i] = v)\n\n# Forward the two methods we promised the package\nBase.similar(u::SpectralField) = SpectralField(similar(u.data))\nBase.copy(u::SpectralField)    = SpectralField(copy(u.data))\n\nBecause SpectralField <: AbstractVector, broadcasting falls back to the standard AbstractArray broadcast machinery and works without further effort. A field type that is not an AbstractArray will need to define a custom BroadcastStyle — see src/couple.jl in this package for a worked example.\n\nThe motivation for going to the trouble of a custom type is everything you do outside Flows.jl: custom convenience constructors, derivative methods, IO, pretty-printing, dispatch on physical meaning. The package itself never inspects the type beyond the three requirements above.","title":"Custom: user-defined state types"},{"category":"section","location":"states/#Explicit-vector-fields","page":"States and vector fields","text":"For explicit integrators (RK4 and the explicit half of any IMEX scheme), the vector field is a callable with signature\n\nf(t::Real, x, dxdt) -> dxdt\n\nArgument Direction Meaning\nt input Current time.\nx read-only input Current state. Do not mutate it; the scheme is still using it.\ndxdt write-only output Buffer to be overwritten with mathrmdmathbfxmathrmdt evaluated at (t, x).\n\nThe return value is conventionally dxdt, but the package never inspects it. Returning nothing is also fine.\n\nf can be any callable: a function, a closure capturing parameters, a callable struct, an anonymous function. The latter two are useful when the right-hand-side carries parameters or owns auxiliary scratch space:\n\nstruct Burgers\n    ν::Float64\n    k::Vector{Float64}                 # wavenumbers, precomputed\n    work::Vector{ComplexF64}           # scratch buffer\nend\n\nfunction (eq::Burgers)(t, u, dudt)\n    # ... use eq.ν, eq.k, eq.work to fill dudt in place ...\n    return dudt\nend\n\nThe work buffer demonstrates a useful pattern: any state your right-hand-side needs but the scheme does not is best owned by the right-hand-side itself.","title":"Explicit vector fields"},{"category":"section","location":"states/#IMEX-vector-fields","page":"States and vector fields","text":"When the dynamics split as dotmathbfx = mathcalLmathbfx + mathbff_mathrmex(tmathbfx), the user provides both pieces.","title":"IMEX vector fields"},{"category":"section","location":"states/#The-explicit-part","page":"States and vector fields","text":"The signature of f_ex is exactly the explicit-vector-field signature above:\n\nf_ex(t, x, dxdt)","title":"The explicit part"},{"category":"section","location":"states/#The-linear-part","page":"States and vector fields","text":"The linear operator mathcalL is not a callable — it is anything that supports two LinearAlgebra-like methods:\n\nLinearAlgebra.mul!(out, A, x)    #  computes out .= A * x\nFlows.ImcA!(A, c::Real, y, z)    #  solves (I - c·A) z = y\n\nmul! is the standard LinearAlgebra interface; the package never patches it. ImcA! is the central primitive of the IMEX schemes: every implicit stage reduces to one ImcA! call with the scheme-dependent constant c. Read the name as \"I-minus-cee-A\".\n\nFor the very common case where the stiff operator is diagonal, the package ships fallback methods that satisfy both signatures for LinearAlgebra.Diagonal. Stiff problems with a diagonal mathcalL — e.g. spectral discretisations of linear diffusion — work without writing any operator-side code:\n\nusing LinearAlgebra\n\n# semi-discretised diffusion in spectral space:\n#   ∂_t û_k = -ν k² û_k + nonlinear(û)\nν = 0.01\nk = collect(0:31)\nL = Diagonal(-ν .* k.^2)               # works out of the box: see src/imca.jl\nf_ex(t, û, dûdt) = (# ... fill dûdt with the nonlinear part)\n\nF = flow(f_ex, L, CNRK2(zeros(ComplexF64, 32)), TimeStepConstant(1e-2))\n\nFor non-diagonal mathcalL, the user provides a struct and two methods:\n\nstruct MyOperator\n    # whatever data describes A\nend\n\nfunction LinearAlgebra.mul!(out, A::MyOperator, x)\n    # out .= A * x\n    return out\nend\n\nfunction Flows.ImcA!(A::MyOperator, c::Real, y, z)\n    # solve (I - c·A) z = y, in place\n    return z\nend\n\nThree notes:\n\nThe same ImcA! method is used for every stage of every IMEX scheme. The constant c changes; nothing else does. Write it once, parameterised by c.\nImcA! should not assume c > 0. Adjoint integrations pass negative c. Code your solver as a generic (I - c·A) solve.\nImcA_mul! has a default. It computes (I - cmathcalL)mathbfy by calling mul! and then doing the axpby in place. Override only if you have a faster route.\n\nThe cookbook has a worked stiff example.","title":"The linear part"},{"category":"section","location":"states/#Coupled-vector-fields","page":"States and vector fields","text":"When the state is itself a Coupled{N} of N components, the right-hand-side is a Coupled{N} of N callables, each with a signature that depends on which other components it sees.\n\nThe default call dependency is \"each component sees itself and every previous one\":\n\ng[1](t, u1, du1dt)\ng[2](t, u1, du1dt, u2, du2dt)\ng[3](t, u1, du1dt, u2, du2dt, u3, du3dt)\n…\n\nThis is the right shape for, e.g., a quadrature appended to a primal system, or a tangent equation appended to a primal system.\n\nf_primal(t, x, dxdt)              = …\nf_tangent(t, x, dxdt, y, dydt)    = …    # depends on x and y\n\nF = flow(couple(f_primal, f_tangent),\n         RK4(couple(zeros(3), zeros(3))),\n         TimeStepConstant(1e-2))\n\nFor non-default coupling patterns — e.g. two independent perturbations both driven by the same primal — the user supplies an explicit CallDependency:\n\n# component 1 depends on itself; 2 and 3 each depend on 1 and themselves\ndeps = CallDependency((1,), (1, 2), (1, 3))\n\nF = flow(couple(f, g1, g2), deps,\n         RK4(couple(zeros(3), zeros(3), zeros(3))),\n         TimeStepConstant(1e-2))\n\nThe signature of g1 is then g1(t, x, dxdt, y1, dy1dt) (sees components 1 and 2), and similarly for g2.\n\nFor the IMEX case, the linear part is also a Coupled{N} whose components are either a linear operator or nothing. nothing marks a component that is advanced fully explicitly — the usual case for a quadrature component. See Coupled systems.","title":"Coupled vector fields"},{"category":"section","location":"states/#What-you-do-*not*-have-to-do","page":"States and vector fields","text":"Three things that other ODE packages require but Flows.jl does not:\n\nYou do not provide an initial-condition object to a constructor. The state is passed to the flow each time it is called, and the schemes hold only buffers, not solutions.\nYou do not subtype AbstractODEProblem or similar. There is no problem object; the vector field and the linear operator are the abstractions.\nYou do not implement an analytical Jacobian. The IMEX path uses the (linear) operator, not the Jacobian. The tangent and adjoint paths require a separately-defined linearisation; see Linearised dynamics.\n\nWith states and vector fields in place, the next page describes how to pick the integration scheme that drives them.","title":"What you do not have to do"},{"category":"section","location":"#Flows.jl","page":"Home","text":"<p align=\"center\">\n  <img src=\"assets/logo.svg\" alt=\"Flows.jl logo\" width=\"640\">\n</p>\n\nA composable, allocation-aware, Julia-native framework for flow-style numerical integration of ordinary differential equations and semi-discretised partial differential equations.\n\nmathbfx(T) = Phi^T-t_0mathbfx(t_0)\n\nFlows.jl is built around the idea that the natural primitive of many algorithms in dynamical systems theory is the flow operator itself — a callable object that maps a state forward by a finite amount of time. Algorithms that fit this idiom include shooting methods, Newton–Krylov continuation of periodic orbits, Floquet analysis, Lyapunov exponent estimation, optimal-control / adjoint-based sensitivity, model-order reduction, and direct numerical simulation drivers. Each of these is more conveniently expressed in terms of repeated, composable calls to a flow than in terms of an opaque \"solve-and-return-trajectory\" routine.","title":"Flows.jl"},{"category":"section","location":"#What-this-package-is","page":"Home","text":"Feature Outcome\nCallable Flows.Flow objects F(x, (t₀, T)) advances x in place. Composable.\nAllocation-free hot path Internal stage buffers are preallocated at scheme construction.\nExplicit and IMEX integrators RK4 plus the low-storage Cavaglieri–Bewley IMEX family.\nCoupled-state support Coupled carries N heterogeneous components and broadcasts elementwise.\nTangent and adjoint linearisations Discretely consistent (stage-cache) and continuous (storage-based).\nSymmetry handling Optional in-place symmetry transformation applied after each flow call.\nTrajectory data structures Monitors, storages (with Lagrange interpolation), stage caches.\nQuadrature-via-coupling Trajectory integrals are first-class, marched alongside the state.","title":"What this package is"},{"category":"section","location":"#What-this-package-is-*not*","page":"Home","text":"It is not a general-purpose ODE/DAE/SDE solver. It does not provide event detection, jump processes, automatic algorithm switching, callback chains, or symbolic problem definitions. The intended user already knows their problem class and the scheme they want to use, and just needs a fast, predictable way to build and compose flow operators.\n\nFor broad, batteries-included differential-equation solving in Julia, see DifferentialEquations.jl. Flows.jl is deliberately narrower and more imperative.","title":"What this package is not"},{"category":"section","location":"#Installation","page":"Home","text":"Flows.jl is not registered in the Julia General registry. Install directly from GitHub: hit ] to enter Pkg mode, then\n\nadd https://github.com/Davide-Lasagna-s-Lab/Flows.jl\n\nThe package depends only on LinearAlgebra, MacroTools, DataStructures, and Printf from the standard library / minimal ecosystem, so it is fast to install and has no heavy transitive dependencies.","title":"Installation"},{"category":"section","location":"#Reading-guide","page":"Home","text":"The documentation is organised as four layers. Read the first two in order; the rest is reference-style and can be browsed on demand.\n\nWhere to read Why\nQuick start Land a working integration in five minutes.\nMathematical foundations Equation classes, flow operators, linearisations, the IMEX splitting.\nArchitecture How Flow, System, AbstractMethod, AbstractTimeStepping, and the symmetry wrapper fit together.\nStates and vector fields Requirements on user types, and what callables the package expects.\nIntegration schemes RK4, CNRK2, and the Cavaglieri–Bewley low-storage IMEX family.\nTime stepping Constant, adaptive, stage-cache-driven, storage-driven.\nCoupled systems Heterogeneous multi-component states and call dependencies.\nTrajectory data Monitors, storages, stage caches, and how they relate.\nLinearised dynamics Tangent and adjoint integration, discrete vs continuous.\nSymmetry transformations Equivariant flows under a continuous group action.\nQuadrature equations Trajectory integrals as coupled components, plus post-hoc rules.\nCookbook End-to-end worked examples.\nInternals Implementation details and design rationale.\nAPI Docstrings for every exported symbol.","title":"Reading guide"},{"category":"section","location":"#Citation","page":"Home","text":"If you use Flows.jl in academic work, please cite the package via its GitHub URL until a software paper is available.","title":"Citation"},{"category":"section","location":"foundations/#Mathematical-foundations","page":"Mathematical foundations","text":"This page sets up the notation and the core mathematical objects that the rest of the manual relies on. Everything here is standard material; the goal is to make the connection between mathematical statements and concrete Flows.jl constructs explicit, so the rest of the manual can move quickly.\n\nIf you only want to use the package and not derive anything, you can skim this page and come back to specific sections when the manual references them.","title":"Mathematical foundations"},{"category":"section","location":"foundations/#State-space-and-dynamical-systems","page":"Mathematical foundations","text":"A state space mathcalX is a Banach space in which the solution of a differential equation lives. In practice mathcalX is one of:\n\nmathbbR^n\nfor a finite-dimensional ODE,\na finite-dimensional approximation of a function space (e.g. Fourier coefficients of a PDE solution),\na Cartesian product of any of the above for coupled systems.\n\nA dynamical system on mathcalX is an evolution equation of the form\n\ndotmathbfx(t) = mathcalLmathbfx(t) + mathbff(tmathbfx(t))qquad mathbfx(t_0) = mathbfx_0inmathcalX\n\nwhere:\n\nmathcalL  mathcalXtomathcalX\nis a linear, time-invariant operator. It carries the stiff part of the dynamics (e.g. diffusion, dissipation, hyperviscosity).\nmathbff mathbbRtimesmathcalXtomathcalX\nis the nonlinear, possibly time-dependent remainder.\n\nIf the problem has no stiffness, take mathcalLequiv 0 and fold everything into mathbff. If the problem is purely linear, take mathbffequiv 0. The splitting is purely operational: it tells the integrator which parts to advance implicitly and which explicitly.","title":"State space and dynamical systems"},{"category":"section","location":"foundations/#The-flow-operator","page":"Mathematical foundations","text":"The flow operator Phi^tau  mathcalXtomathcalX maps an initial condition forward by an interval tau:\n\nmathbfx(t_0 + tau) = Phi^taumathbfx(t_0)\n\nThe two-parameter family Phi^tau_tauge 0 inherits the semigroup property\n\nPhi^tau_1+tau_2 = Phi^tau_2circPhi^tau_1\n\nwhich is the structural reason every algorithm that walks a trajectory in pieces (shooting, time-marching, sub-stepping) is expressible in terms of Phi^tau alone.\n\nA Flows.Flow is a discrete approximation of Phi^tau: it is a callable object built around a one-step scheme step! and a time-step iterator that subdivides tau.\n\nF = flow(f, RK4(zeros(3)), TimeStepConstant(0.01))\nF(x, (t₀, T))    # produces an approximation of Φ^{T - t₀} x in place\n\nThe integration error is determined by the order of the scheme and the step size, not by the size of the interval tau — the iterator simply concatenates many one-step calls.","title":"The flow operator"},{"category":"section","location":"foundations/#Time-discretisation:-Butcher-form","page":"Mathematical foundations","text":"Flows.jl builds every integration scheme out of one or two Butcher tableaux. For an explicit Runge–Kutta method with s stages the update reads\n\nbeginaligned\nmathbfY_k = mathbfx_n + Delta tsum_j=1^k-1 a_kjmathbff(t_n + c_jDelta tmathbfY_j)quad k = 1dotss\nmathbfx_n+1 = mathbfx_n + Delta tsum_k=1^s b_kmathbff(t_n + c_kDelta tmathbfY_k)\nendaligned\n\nwith coefficients packed in a matrix A=(a_kj) and vectors mathbfb mathbfc. An embedded weight vector mathbfe is also stored alongside mathbfb so that an error-estimating sibling scheme can be evaluated at no extra stage cost — useful for adaptive variants, even though the schemes shipped today are not themselves adaptive.\n\nFor IMEX schemes, two tableaux travel together — one for the implicit part of mathbff=mathcalLmathbfx+mathbff_textex and one for the explicit part. The implicit stages take the form\n\nmathbfY_k = mathbfx_n + Delta tsum_j=1^k-1 a_kj^mathrm ImathcalLmathbfY_j + Delta ta_kk^mathrm ImathcalLmathbfY_k + Delta tsum_j=1^k-1 a_kj^mathrm Emathbff_textex(cdots)\n\nwhich, rearranged, requires solving the linear problem\n\n(I - Delta ta_kk^mathrm ImathcalL)mathbfY_k = mathbfx_n + Delta tsum_j=1^k-1 a_kj^mathrm ImathcalLmathbfY_j + Delta tsum_j=1^k-1 a_kj^mathrm Emathbff_textex(cdots)\n\nThe user supplies mathcalL via its action mul!(out, A, x) and a method for ImcA! that solves (I - cmathcalL)mathbfz = mathbfy. The constant c depends on the scheme and the stage; both the schemes and the solver are oblivious to its value, so the user writes a single c-parameterised routine that the integrator drives.\n\nThe implemented tableaux are stored in src/tableaux.jl as exact rationals and converted to Float64 once at module load. See Integration schemes for the catalogue.","title":"Time discretisation: Butcher form"},{"category":"section","location":"foundations/#Tangent-equations","page":"Mathematical foundations","text":"Suppose the trajectory mathbfx(t) is perturbed at t=t_0 by a small mathbfy_0inmathcalX. To first order in mathbfy_0, the perturbation mathbfy(t) propagates according to the tangent equation\n\ndotmathbfy(t) = mathcalLmathbfy(t) + mathbff_mathbfxbigl(tmathbfx(t)bigr)cdotmathbfy(t)qquad mathbfy(t_0) = mathbfy_0\n\nwith mathbff_mathbfx the Jacobian of mathbff along the nonlinear trajectory mathbfx(t). This is a linear, non-autonomous problem whose coefficients depend on the primal solution.\n\nThe corresponding flow Jacobian is\n\nJ^tau(mathbfx_0) = fracpartialPhi^taupartialmathbfx_0(mathbfx_0)\n\nso that mathbfy(t_0+tau) = J^tau(mathbfx_0)mathbfy_0. Flows.jl does not build J^tau as a matrix; instead it constructs a tangent flow operator whose action on mathbfy_0 is the matrix–vector product J^taumathbfy_0.\n\nTwo tangent integration paths are provided. They are discrete (replay the primal stages stored in a RAMStageCache) or continuous (interpolate the primal trajectory from a RAMStorage); see Linearised dynamics for the trade-offs.","title":"Tangent equations"},{"category":"section","location":"foundations/#Adjoint-equations","page":"Mathematical foundations","text":"The adjoint of the tangent operator J^tau is, with respect to a chosen inner product langlecdotcdotrangle on mathcalX, the unique linear operator (J^tau)^* satisfying\n\nbigllanglemathbfpJ^taumathbfybigrrangle = bigllangle (J^tau)^*mathbfpmathbfybigrranglequadforallmathbfpmathbfyinmathcalX\n\nThe adjoint state mathbfp(t) obeys the adjoint equation\n\n-dotmathbfp(t) = mathcalL^*mathbfp(t) + biglmathbff_mathbfxbigl(tmathbfx(t)bigr)bigr^*mathbfp(t)\n\nwhich differs from the tangent equation in three ways: it runs backwards in time (initial condition specified at t=T), it uses the transpose of the Jacobian, and it depends on the same primal trajectory mathbfx(t).\n\nAdjoint methods are the workhorse of gradient-based optimisation in dynamical systems: one backward sweep yields the gradient of an objective mathcalJ(mathbfx_0) with respect to all components of mathbfx_0 at the cost of roughly one extra forward integration, regardless of the dimension of mathcalX. See the Lorenz adjoint sensitivity entry in the Cookbook for an end-to-end derivation.","title":"Adjoint equations"},{"category":"section","location":"foundations/#Discrete-vs-continuous-adjoints","page":"Mathematical foundations","text":"A continuous adjoint is the analytical derivation above, discretised on the integrator's grid after the math is done. A discrete adjoint is what you get by mechanically transposing the integrator's update formulas. The two agree as Delta tto 0 but can differ at finite Delta t.\n\nFlows.jl supports both:\n\nContinuousMode(true) selects the continuous adjoint, driven by an interpolant over a RAMStorage.\nDiscreteMode(true) selects the discrete adjoint, driven by stage values stored in a RAMStageCache.\n\nWhen the downstream optimisation algorithm relies on the gradient being an exact derivative of the discretised objective (e.g. line searches with strict Armijo conditions, second-order methods), the discrete adjoint is the safer choice. Otherwise the continuous adjoint is usually fine and is much cheaper in memory when the storage degree is low.","title":"Discrete vs continuous adjoints"},{"category":"section","location":"foundations/#Quadrature-integrals-along-a-trajectory","page":"Mathematical foundations","text":"Many quantities of interest are not values of the state at a single time but integrals along a trajectory:\n\nI(T) = int_t_0^T gbigl(mathbfx(t)bigr)mathrmdt\n\nFlows.jl supports two ways of computing such integrals:\n\nCouple the quadrature into the system. Append a new component I(t)inmathbbR^m whose dynamics are dotI(t) = g(mathbfx(t)), and integrate the augmented state (mathbfxI) as a Coupled. The integral converges at the order of the time integrator. This is the preferred path.\nPost-process samples. Record g(mathbfx(t)) in a Monitor and apply trapz or simps at the end. Convenient when g is decided after the integration is done, but the order of accuracy is fixed by the quadrature rule, not the time integrator.\n\nThe coupled path is treated in Quadrature equations.","title":"Quadrature integrals along a trajectory"},{"category":"section","location":"foundations/#Symmetry-transformations","page":"Mathematical foundations","text":"Some dynamical systems are equivariant under a continuous group action: if g_s  mathcalXtomathcalX is a one-parameter family of symmetries indexed by s, then\n\nPhi^taubigl(g_smathbfxbigr) = g_sPhi^taumathbfxqquadforalltaus\n\nA common use case is reducing a translation- or rotation-invariant problem by post-composing the flow with a symmetry parameterised by a phase s chosen by the user — e.g. to track a relative periodic orbit.\n\nFlows.jl accepts an optional callable sym(x, s) at flow-construction time that is applied to the post-integration state. The wrapper is Flows.SymTransform for single states and Flows.CoupledTransform for coupled states. See Symmetry transformations for the workflow.","title":"Symmetry transformations"},{"category":"section","location":"foundations/#Notation-used-in-the-rest-of-the-manual","page":"Mathematical foundations","text":"Symbol Meaning Code object\nmathcalX State space the type of x\nmathbfxmathbffmathcalL State, vector field, linear operator x, f, A\nPhi^tau True flow operator F::Flow is the discrete approximation\nDelta t Time step ts.Δt for TimeStepConstant(Δt)\nmathbfY_k k-th internal stage element of method.store\na_kj b_k c_k Tableau coefficients tab[:a, k, j], tab[:b, k], tab[:c, k]\nmathbfymathbfp Tangent and adjoint states y, q in user code\nJ^tau Flow Jacobian not constructed; only its action is computed\nI(T) Trajectory integral coupled component or post-hoc trapz/simps\ng_s Symmetry action with parameter s sym(x, s) callable wrapped in SymTransform\n\nWith the equations and the mapping to code in hand, the next page — Architecture — describes how these pieces fit together inside a single Flows.Flow object.","title":"Notation used in the rest of the manual"}]
}
